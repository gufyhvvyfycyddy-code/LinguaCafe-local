# LinguaCafe 当前工作台 / Codex 交接临时文档

> **最后更新**：2026-07-07 (GM52-AIStudyCardPendingLifecycleClosure-1000-5)
> **文档入口**：先读 `docs/DOCUMENTATION_INDEX.md`，再读本文。
> **旧交接文档**：`docs/CODEX_HANDOFF.md`（2026-06-23）和 `docs/handovers/2026-06-24-c12-c-handoff.md` — 这些是历史交接文档。Codex 新任务应以本文为准。
> **历史索引**：`docs/HISTORY_INDEX.md` 记录旧 status / next task / FSRS phase 文档，避免上下文污染。

---

## 1. 当前阶段一句话

架构收口阶段已结束（总体架构收口 100%）。AI 示意卡 V1-V4 已实现。近期已新增多例句池、复习页题面例句轮换、答案后补充例句不重复、多来源溯源 carousel、Finished reading 确认弹窗、词形原型绑定（surface 保留 + lemma 优先搜索 + 用户修正后生效）、已学词义候选面板、熟词僻义前置结构（不调 AI）、例句池性能优化（消除 N+1 + 批量预加载）、真实 tokenizer/importer 端到端覆盖、8 类形态真实页面点击、词性歧义真实点击验证、阅读页 inline sense preview 面板、阅读中词义确认持久化（ADR-0003 + additive-only 表 + match/not_match upsert）、阅读中确认结果使用层（match_count/not_match_count/last_choice/recent_examples 聚合 + 预览面板使用层显示块）。本轮新增阅读中确认记录管理入口：`ReadingInlineSenseConfirmationService::listConfirmationsForManagement()`/`revokeConfirmation()` 只读列表 + 单条撤销（DELETE 操作，仅删除当前用户/当前语言单条 confirmation 记录），新增 `GET /senses/inline-confirmations` 列表端点（choice/lemma/surface/word_sense_id/chapter_id/date_from/date_to 筛选 + 分页 + WordSense/chapter/sentence 摘要 + 严格 user/language 隔离）+ `DELETE /senses/inline-confirmations/{id}` 撤销端点（返回 `revoked:true` + 6 条 safety_flags：no_review_log_created/no_fsrs_changed/no_review_card_changed/no_word_sense_deleted/no_review_card_deleted/not_a_review_rating），新增 `ReadingInlineConfirmationManage.vue` 管理页（`/senses/inline-confirmations/manage`：筛选 全部/是这个意思/不是这个意思/lemma/文章 + 列表 surface/lemma/choice/WordSense 摘要/来源句子/文章/最近更新时间 + 「查看来源」/「撤销这条记录」操作 + 「这不是复习评分」/「不会写入复习记录」/「不会改变复习进度（FSRS）」/「不是忘记，不是复习失败，也不是删除词义」安全文案 + 撤销确认弹窗 + 空状态文案），`InlineSensePreviewPanel.vue` 新增「查看全部阅读确认记录」入口链接到管理页。49 个后端护栏测试（list 端点 user/language 隔离 + choice/lemma 筛选 + WordSense 摘要 + revoke 仅删当前 user/language 单条 + 不写 ReviewLog/FSRS/ReviewCard/WordSense 删除 + 未知 id 安全失败）+ 19 个 UI guard 测试（文件存在 + 标题/不是复习评分/不会写入复习记录/不会改变复习进度/是这个意思/不是这个意思/撤销这条记录 文案 + 撤销弹窗 + 不是忘记/不是复习失败/不是删除词义 文案 + 无 FSRS rating 按钮 + 无 rating/review/fsrs 路由 + 无 AI 路由 + 仅调用安全端点 + 无禁止撤销含义文案 + 无批量撤销 UI + preview panel 链接到管理页 + 回到阅读页链接 + 空状态文案）。R4 morphology 轮 13 个真实 Playwright 点击（windows/horses/sheep/mice/deer/broke/drove/spoken/threw/grown/taller/walked/finished，11/13 正确，2 P2 residual 在 V-ed 歧义形式与 R2 一致，0% 重复率 vs R3，8/8 类别覆盖，tokenizer 经不规则形式 mice→mouse/broke→break/drove→drive/spoken→speak/threw→throw/grown→grow 确认可用），MCP Chrome 全流程验收（POST inline-confirmation → GET manage list → filter choice=match → DELETE revoke → list 更新 → re-click token 不再显示 persisted_choice），网络仅 inline-confirmation POST + inline-confirmations GET + DELETE revoke，无 ReviewLog/FSRS/AI。未写 ReviewLog、未改 FSRS、未真实调用 AI、未实现阅读中刷卡评分、未自动创建 WordSense/ReviewCard、未新增 migration、未删除 WordSense/ReviewCard/ReviewLog、未批量撤销、未实现评分按钮。1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。下一步仍应由网页端总设计师选择，不自动进入 AI 真实推荐、AI 释义生成或完整闭环，也不自动进入阅读中刷卡。

**GLM-SenseReviewRealSmoke-1000-1**：SenseReview 词义复习主线真实刷卡 smoke 与每日上限入口最小修复。用 MCP Chrome 真实浏览器在 `/reviews/senses` 刷 5 张 sense cards（card_id 57/69/64/67/70，覆盖 good/hard/again/easy/good），验证查看答案、评分按钮、Space/1/2/3/4 快捷键、More 菜单 5 项（查看原文/编辑/归档/重置/彻底删除）、查看原文对话框（非 fallback，已定位到原文位置）、空状态文案。5 条 ReviewLog（log_id 9-13）全部 `source=sense_review`，无重复写入，5 个 WordSense 保持 confirmed，confirmed_word_senses=13 一致，user/language 隔离正确。发现 P1：`SenseReview.vue` 前端只使用 `summary.due_count`，不显示 `limit_message` 和超额入口，导致达到每日上限后用户无法继续复习隐藏的到期卡。最小修复（仅 `SenseReview.vue`）：新增 `ignoreDailyLimits` data 状态；`loadCards()` / `rate()` 方法支持传递 `ignoreDailyLimits` 参数；统计区新增 `limit_message` info alert（基于 `summary.limit_message`）+ 「继续复习超额卡片」按钮（基于 `summary.can_continue_over_limit`）+ `ignoreDailyLimits` 模式下的 warning alert + 「恢复上限」按钮；新增 `continueOverLimit()` / `restoreLimits()` 方法。MCP Chrome 真实验收 P1 修复：临时构造 daily_review_limit=1 + card_id=56 重新到期场景（marker=`__VG_SENSE_REVIEW_SMOKE__`），验证 limit_message 显示「今天已完成 5 张复习，达到每日复习上限。还有 1 张到期卡暂时未显示。」+ 「继续复习超额卡片」按钮 → 点击后 `GET /reviews/senses?ignoreDailyLimits=true` 返回 1 张卡 + 「恢复上限」按钮 → 点击后 `GET /reviews/senses` 返回 0 张卡 + limit_message 重新显示，Network 仅 3 个 GET 请求，Console 仅预期 WebSocket 降级，验证后立即恢复临时数据（card_id=56 fsrs_due_at 恢复、临时 daily_review_limit setting 删除）。自动测试：SenseReview 19 passed / ReviewCardManageDangerContractTest 13 passed / WordSense 193 passed / ReviewCard 288 passed / FsrsSchedulingServiceTest passed / TestingDatabaseHealthConfigTest+TestingDatabaseHealthTest passed / npm run development 编译成功；ReviewFsrsTest 1 个预存在失败（`sense review sentence tokens do not cross user scope`，legacy `/reviews` word review 端点测试，不是 sense review 端点，不在本轮修复范围）。smoke playbook 新增 Hotkey Acceptance + Daily Limit Acceptance 章节。不改 FSRS 算法 / ReviewLog 逻辑 / AI / inline confirmation / ReviewCardManage / Source Context fuzzy。不新增 migration。不自动进入下一任务。1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。

**GLM-ReviewPayloadSourceOwnershipHardening-1000-1**：Review payload 来源所有权护栏修复。修复 `ReviewFsrsTest::test_sense_review_sentence_tokens_do_not_cross_user_scope` 暴露的跨用户来源句泄漏 P1 风险。根因：`WordSenseExamplePoolService::exampleCandidates()` 在 batch-loaded chapters 按 user_id+language 过滤后找不到跨用户/跨语言 chapter 时，仍然把 `occurrence.sentence_en` 加入 candidates，导致 `SenseReviewCardSerializerService::serialize()` 第 50 行用不可信 occurrence 的 sentence_en 作为题面 `example_sentence_en`，同时 `example_candidates` 数组也直接返回给前端。最小修复（仅 `app/Services/WordSenseExamplePoolService.php`）：在遍历 occurrences 时新增 ownership guard — 当 `occurrence.chapter_id !== null && !isset($chaptersById[$occurrence->chapter_id])` 时 `continue` 跳过该 occurrence，不加入 candidates，不进入题面/补充例句/synthetic fallback。fallback 仍走 `WordSense.example_sentence_en`（card_example candidate）。`SenseTokenPayloadService` 经审查安全（Layer 1/2 chapter 查询已按 user_id+language 过滤，Layer 3 synthetic 用 `$sense->example_sentence_en`），无需修改。`SenseSourceContextService::sourceContext/sourceContextList` 经侦察安全（sourceContextList 第 141-144 行 `$chapter` 为 null 时 continue 跳过；sourceContext 用 `resolver->findChapterById($chapterId, $sense->user_id, $sense->language_id)` 过滤），无需修改。新增 7 个 contract tests（WordSenseExamplePoolTest：跨用户 chapter 跳过 + 跨语言 chapter 跳过 + invalid chapter_id 跳过 + chapterless occurrence 保留为 occurrence-only + supplementary 不来自不可信 occurrence；ReviewFsrsTest：`/reviews/senses` 端点跨用户不泄漏 + `/reviews` 端点 fallback 到 WordSense.example_sentence_en 且 token_source=synthetic）+ 修改 1 个现有测试（`test_pool_chapter_title_resolves_null_when_chapter_belongs_to_other_user` 从期望保留 foreign occurrence 改为期望跳过，收紧安全边界而非降低断言强度）。`/reviews` 与 `/reviews/senses` 双入口均覆盖（共用 `SenseReviewCardSerializerService::serialize`）。自动测试：TestingDatabaseHealthConfigTest 6 passed / TestingDatabaseHealthTest 6 passed / ReviewFsrsTest 63 passed（从 1 failed 变为全绿）/ SenseReview 19 passed / WordSenseExamplePoolTest 26 passed / ReviewCard 288 passed / ReviewLog 1 passed / WordSense 197 passed / ReviewCardManageDangerContractTest 13 passed / npm run development 编译成功。MCP Chrome `/reviews/senses` smoke：页面正常显示空状态，Network 仅 3 个 GET（reviews/senses + review-cards/stats + fonts），Console 仅预期 WebSocket 降级。不改 FSRS 算法 / ReviewLog 写入 / ReviewCardManage / inline confirmation / AI / tokenizer / migration。不自动进入下一任务。1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。

## 2. 最近已完成任务

| 任务 | 一句话说明 |
|------|-----------|
| FsrsReschedulePreviewService-ContractScouting-1 | 只读侦查 FSRS 重排预览/确认/应用链路，输出 18 个风险点 |
| FsrsReschedulePreviewService-GapContractTests-1 | 补 5 个 preview + 5 个 confirmPreflight 缺口契约测试 |
| FsrsRescheduleGapContractTests-ScopeFix-1 | 收口越界修改的测试文件 |
| TextBlockService-CreateNewEncounteredWordsContractTests-1 | 12 个 characterization tests 锁定 encountered_words 创建行为 |
| EncounteredWordCreationService-Extract-1 | 从 TextBlockService 提取 encountered_words 写入到独立 Service |
| WordSenseService-DestroyRestore-RiskAudit-1 | 审计 WordSense 删除/归档/恢复链路，输出 14 个风险点 + 总设计师复判 |
| DesignerWorkflow-CodeBuddyRiskRoleAndPlanRefresh-1 | 修正 CodeBuddy 风险角色规则 + 大计划修正 |
| WordSenseService-DestroyRestoreContractTests-1 | 15 个 contract tests 锁定归档/删除/恢复语义 |
| ReviewCardDeleteSnackbar-HistoryPreservedCopy-1 | 补管理页删除成功 snackbar/fallback 文案，MCP Chrome 验收 |
| Codex-ArchitectureOptimizationLoop-1 | Codex 增量架构总审计；新增 5 个 TextBlock phrase/index characterization tests；更新 master plan / hotspot audit / 当前工作台 |
| Codex-ArchitectureFinalGoalMode-1 | Codex 面向 sense-only 最终架构目标做增量审计；补 2 个 FSRS confirmAndApply 拒绝写入 contract tests；更新 master plan / hotspot audit / 当前工作台 |
| OpenCode-ArchitectureTargetMode-Batch1 | 综合推进：SenseReview 到期卡真实显示并评分（MCP Chrome）、TextBlock fallback 只读侦查、ReviewCardManage logs payload 只读侦查。阶段性完成，仍有缺口：pending occurrence 写入未跑通、current-working-handoff 上轮漏更新、TextBlock fallback 缺测试、logs payload 缺 contract tests。 |
| Codex-SpecToHarnessHardeningTargetMode-1 | 将两个软规则转成测试护栏：新增 TextBlock fallback tokenizer 单元测试；补 ReviewCardManage logs payload 精确字段/日期格式与同卡 user/language 过滤 contract tests；不改业务代码。 |
| Codex-SenseReviewRealWorkflowHardeningTargetMode-1 | 将 SenseReview 真实页面 smoke 转为可复验 harness：新增 `smoke:sense-review-data` 命令（只接受已有用户、不创建账号、不接收密码、marker 可识别、不清库）；新增命令 feature test 锁定 marker 形状；新增 smoke playbook。MCP Chrome 真实验收覆盖评分/More/查看原文/确认/改绑/拒绝/忽略/新建。不改 Vue/FSRS/WordSense 语义/ReviewLog/DB schema/AI study card。 |
| Codex-FinalArchitectureClosureTargetMode-1 | 最终架构收口。新增三份冻结文档：`final-architecture-closure-report.md`（收口报告，总体架构收口 81%→100%）、`ai-study-card-v1-frozen-plan.md`（AI 示意卡第一版路线冻结，AI 示意卡规划 25%→55%）、`frontend-review-entry-unification-plan.md`（前端复习入口统一路线冻结，前端入口整理 50%→65%）。MCP Chrome 只读复核阅读页/查词侧栏/AI 阅读辅助按钮/复习入口/词义确认入口/复习卡管理入口。不改业务代码、测试、Vue、Controller、Service、routes、migration、DB schema。 |
| Codex-AIStudyCardV1-And-ReviewEntryUnification-1 | AI 示意卡第一版最小实现 + 前端复习入口统一第一轮。新增 `ai_study_card_pending_items` pending 表、Model/Service/Controller/POST route、侧栏「待 AI 解释」按钮、幂等与隔离 tests；首页「开始复习」和导航「复习」进入 `/reviews/senses`，旧 `/senses/review`、`/review-cards/manage`、`/review/false/-1/-1` 保留。不调用 AI、不生成 WordSense/ReviewCard/ReviewLog、不改 FSRS/删除归档恢复。 |
| GLM-AIStudyCardV2-GenerationLoop-1 | AI 示意卡 V2 生成闭环第一阶段。新增 `GET /ai-study-card/pending-items`（支持 chapter_id 过滤）、`POST /ai-study-card/pending-items/{id}/dismiss`、`POST /ai-study-card/pending-items/{id}/restore`；改造 `createOrGetPending` 支持 dismissed 恢复；在 `VocabularySideBox.vue` / `VocabularyBox.vue` 新增待解释列表面板、取消按钮、生成前预览弹窗雏形。新增 16 个 V2 feature tests（23 tests / 105 assertions 全绿）。MCP Chrome 真实页面验收 24 项全通过。不调用 AI、不生成 WordSense/ReviewCard/ReviewLog、不改 FSRS/删除归档恢复。 |
| GLM-AIStudyCardV3-SafePreviewPackage-1 | AI 示意卡 V3 安全生成包。扩展 `GET /ai-study-card/pending-items` 支持 `status=pending\|dismissed\|all` 过滤；新增 `POST /ai-study-card/pending-items/preview-package` 后端安全包接口（schema_version=ai-study-card-preview-package-v1，含 selected_items/generation_rules/safety_flags）；在 `VocabularySideBox.vue` / `VocabularyBox.vue` 新增待解释/已取消视图切换、已取消项恢复按钮、真实预览弹窗（用户已选词列表/来源句子/章节位置/勾选取消/全不选禁用生成/AI 推荐词占位/安全说明/生成规则）、「准备生成」按钮触发后端安全包、JSON 展示与复制按钮。新增 14 个 V3 feature tests（37 tests / 184 assertions 全绿）。MCP Chrome 真实页面验收 28 项全通过。不调用 AI、不生成 WordSense/ReviewCard/ReviewLog、不改 FSRS/删除归档恢复。 |
| GLM-AIRecommendationConfirmationLoop-V4-1 | AI 示意卡 V4 AI 推荐词确认闭环。新增 `POST /ai-study-card/pending-items/final-candidates-package` 后端接口（schema_version=ai-study-card-final-candidates-v1，含 user_selected_items/ai_recommended_selected_items/ai_recommended_unselected_items/dedupe_summary/generation_rules 5条/safety_flags 6条；三重隔离 + 后端二次去重 + 空结果 422 + 数量上限保护）；在 `VocabularySideBox.vue` / `VocabularyBox.vue` 新增 V4 完整 UI（粘贴 AI 推荐词 JSON 文本框 + 解析/清空按钮 + 解析错误提示 + 解析摘要 + AI 推荐词列表默认 unchecked + 全选/全不选 + 用户已选词/AI 推荐词视觉分区 + 「生成最终候选包」按钮 + JSON 展示与复制按钮）。新增 18 个 V4 feature tests（56 tests / 294 assertions 全绿）。MCP Chrome 真实页面验收 33 项全通过。不调用 AI、不生成 WordSense/ReviewCard/ReviewLog、不改 FSRS/删除归档恢复。 |
| Codex-LegacyEntry-FinishedReading-ExampleGuard-1 | 旧版入口清理执行 + Finished reading 安全护栏 + 阅读例句路线冻结。普通查词组件不再展示“旧词条释义 / 旧版释义 / 旧版示意 / legacy word review”入口文案；后端 legacy 兼容层、`ReviewCard::TARGET_WORD`、legacy route/service/tests 保留。新增 `FinishedReadingSafetyTest` 锁定 Finished reading 只把当前用户/当前语言 `stage=2` 黄词设为 known，且不改绿词 stage、WordSense、ReviewCard、ReviewLog、FSRS；发现并修复自动 known 分支缺少 `language` 过滤。新增 `LegacyEntryUiGuardTest` 防止旧入口文案回到普通查词组件。新增 `reading-inline-review-and-example-pool-plan.md`，只冻结阅读中刷卡/多例句轮换路线，不实现。 |
| Trae-ExamplePool-ReviewRotation-SourceCarousel-1 | 多例句池 + 复习页题面例句轮换 + 答案后补充例句不重复 + 多来源溯源 carousel + Finished reading 确认弹窗。新增 `WordSenseExamplePoolService`（复用 `WordSenseOccurrence` + card example fallback，不新增 migration，不调用 AI），`SenseReviewCardSerializerService` payload 新增 `example_candidates` / `example_candidates_count` / `supplementary_example`，稳定 seed 轮换（review_card_id + fsrs_reps + day-of-year，crc32）；`SenseSourceContextService` 新增 `sourceContextList` 方法 + 新路由 `GET /senses/{id}/source-context-list`，`SenseExampleDialog.vue` 支持来源 1/N 切换；`TextReader.vue` 「完成阅读」按钮新增确认弹窗（说明影响 + 取消/确认）。新增 18 个 feature tests（WordSenseExamplePoolTest 12 + SenseSourceContextMultiSourceTest 6，全绿）。MCP Chrome 真实页面验收：登录 → /reviews/senses 题面正常 → 查看答案 → 单例句无重复补充 → 单来源无切换按钮（符合规则） → 阅读页完成阅读确认弹窗 → 取消不执行 → console/network 正常。**阅读中刷卡仍未实现；AI 不生成例句。** |
| Trae-LemmaKnownSenseBridge-1 | 词形原型绑定 + 已学词义候选 + 熟词僻义前置结构 + 例句池性能优化。新增 `WordSenseKnownSenseService`（只读：`listConfirmedSensesForLemma` + `knownSenseLookupPayload`，批量预加载 occurrence count，不写 ReviewLog/WordSense/ReviewCard/FSRS，不调 AI）；新增端点 `GET /senses/known-sense-lookup?lemma=...&language=...`；`WordSensesList.vue` 新增「已学词义候选」面板（confirmed WordSense 列表，含 sense_zh/sense_en/pos/FSRS chip/复习次数）和「熟词僻义」前置 info alert（明确标注「未调用 AI 判断」）；`WordSenseExamplePoolService::exampleCandidates()` 消除 N+1（批量 whereIn 预加载 Chapter）；`SenseSourceContextService::sourceContextList()` 消除循环内 findChapterById（批量 whereIn 预加载 + limit 12 + PHP unique+take(3) 保持跨数据库兼容）。surface/lemma 绑定：阅读页点词显示 surface（如 geese）+ lemma（如 goose）+ [修改] 入口；搜索/添加新释义优先使用 lemma（搜索框 value=lemma）；用户通过 VocabularySideBox saveLemma 修正 lemma 后，后续添加新释义使用修正后的 lemma；不无条件绑定 published→publish。新增 13 个 feature tests（WordSenseKnownSenseBridgeTest）。MCP Chrome 真实页面验收：登录 → 阅读页点击 geese → 显示 geese→goose→[修改] → 添加新释义搜索框 value=goose → 创建 confirmed sense 后重新点击 geese → 「已学词义候选」面板显示 + 「熟词僻义」提示显示 → /senses/known-sense-lookup 端点 200 → 复习页显示答案 + 例句正常 → 查看原文多来源溯源正常 → console 仅预期 WebSocket 降级 → network 全 200。**阅读中刷卡评分仍未实现；AI 不生成例句；不写 ReviewLog；不改 FSRS；熟词僻义仅前置结构，AI 判断仍未实现。** |
| Codex-MorphologyMatrix-ImportRegression-1 | 形态变化测试矩阵 + 文章 fixture 导入回归 + lemma bridge 覆盖增强。新增 `MorphologyMatrixImportRegressionTest`：覆盖规则复数（ways/technologies）、不规则复数（mice/children）、第三人称（studies/watches）、过去式（ran/went）、过去分词（written/published）、进行时（running/studying）、比较级/最高级（better/worse）、词性歧义（used/broken）；项目可控文章 fixture 的 `processed_text` 保留 surface/lemma/pos；known-sense lookup 只按 lemma 返回 confirmed WordSense，保持 user/language/status 隔离，`read_only=true`，不写 ReviewLog，不创建 ReviewCard，不改 FSRS；`published/running/used/broken` 等歧义词不自动绑定、不自动刷卡。新增 `MorphologyMatrixUiGuardTest` 锁定 `WordSensesList.vue` 仍显示 surface+lemma、add-sense payload 仍用 `lemma: effectiveLemma` + `surface_form: surfaceWord`、熟词僻义文案仍标注未调用 AI、旧入口仍隐藏。**阅读中刷卡评分仍未实现；AI 判断熟词僻义仍未实现；不写 ReviewLog；不改 FSRS；AI 不生成例句。** |
| GLM-RealMorphologyImportClickCompletion-1 | 真实 tokenizer/importer 覆盖 + 8 类形态真实点击与页面验收补完。将旧 `MorphologyMatrixImportRegressionTest`（硬编码 fixture）重命名为 `MorphologyMatrixLemmaBridgeDataLayerTest`，新增真正的 `MorphologyMatrixImportRegressionTest`：真实调用 `ChapterService::processChapterText()` → Python spaCy tokenizer（127.0.0.1:8678）→ `processTokenizedWords()` → `setProcessedText()`，验证真实 `processed_text` 包含 surface/lemma/pos/sentence_index；覆盖 8 类形态各 ≥2 词 + 词性歧义 4 词（published/used/broken/left）；验证 published/used/broken/left 不自动绑定、不写 ReviewLog、不创建 ReviewCard、不改 FSRS；known-sense lookup 保持 `read_only=true`。MCP Chrome（Playwright）真实页面点击 18 个 token，8/8 类别覆盖，4 个词性歧义真实点击，每个词记录 surface/lemma/searchBox/knownSenseCandidates/legacyEntry/AI judgment；`published` 显示"已学词义候选"+"未调用 AI 判断"；无 API/axios/fetch 替代。**阅读中刷卡评分仍未实现；AI 判断熟词僻义仍未实现；不写 ReviewLog；不改 FSRS；AI 不生成例句；705% 是子阶段提升，不是固定五条主线虚假上涨。** |
| GLM-MorphologyLemmaDefectFix-1 | 修复 OpenCode-RealClickFinalAudit-3 暴露的 morphology lemma 缺口。根因定位：Python tokenizer 本身正确，缺口在 PHP fallback `TextBlockService::conservativeFallbackLemma()` 原本只查不规则表，其他全部返回 surface，导致 Python 服务不可用时 `technologies/watches/stories/bodies/fixes/boxes` 等 lemma 错误。修复方式（可解释规则，非硬编码本轮测试词）：`lemmaInEcdict()` / `ecdictAvailable()` 改为 protected；新增 ECDICT-gated `-ies → -y/-ie` 规则（仅在 ECDICT 验证 `technology/story/body` 等候选 lemma 真实存在时才还原），覆盖 technologies/stories/bodies；新增 ultra-safe `-ches/-shes/-xes/-zes → strip -es` 规则（不查 ECDICT，确定性后缀剥离），覆盖 watches/fixes/boxes/goes。同时修复测试用户 stale encountered_words 条目（technologies/watches/published 之前 Python 宕机时创建的错误 lemma）。新增 `tests/Feature/MorphologyLemmaDefectFixTest.php` 6 个测试方法 / 149 断言（真实 Python tokenizer），扩充 `tests/Unit/TextBlockFallbackTokenizerTest.php` 4 个新测试方法（PHP fallback 路径）。MCP Chrome（Playwright）真实页面点击 15 次：13 OK + 2 P2 残留（`has broken→break` 因 EncounteredWord 单 lemma 设计限制未还原；`left side→left` 因同上限制；二者均按 spec 5.3 A/B 报告为 P2，不强制修改）。旧入口核查：未出现 `旧词条释义/旧版释义/旧版示意/legacy word review`。ReviewLog/ReviewCard/WordSense 点击前后计数一致（read-only 验证）。**阅读中刷卡评分仍未实现；AI 判断熟词僻义仍未实现；不写 ReviewLog；不改 FSRS；AI 不生成例句；不真实调用 AI；不删除 legacy 兼容层；不硬编码本轮测试词作弊；700% 是子阶段提升，不是固定五条主线虚假上涨。** |
| GLM-ReadingInlinePreview-First-1 | 阅读页 WordSense preview / 候选绑定确认第一轮。新增 `WordSenseKnownSenseService::previewInlineSenseCandidates()` 只读方法（返回 surface/lemma/sentence/candidates/safety_flags 6 条：read_only/no_review_log_created/no_fsrs_changed/no_review_card_created/no_word_sense_created/no_ai_called）；新增 `GET /senses/inline-preview` endpoint（只读，enforces user/language isolation，422 on empty lemma，403 on language mismatch）；新增 `InlineSensePreviewPanel.vue` 只读子组件（显示候选预览 + safety banner + 「是这个意思/不是这个意思」按钮仅修改前端 userChoice data，不 POST/PUT/DELETE）；`WordSensesList.vue` 嵌入该组件。新增 31 个 guard tests（InlineSensePreviewTest 20 + InlineSensePreviewUiGuardTest 11）：证明 endpoint 不写 ReviewLog/FSRS/WordSense/ReviewCard、不调用 AI、只返回当前用户+语言 confirmed senses、排除 pending/ignored/rejected、跨用户/跨语言隔离、空 lemma 安全空态；UI guard 证明 preview/safety 文案存在、无「立即评分」/肯定性「写入复习记录」/「AI 已判断」、不触发 rating route。MCP Chrome 真实验收 5 个 token（men/women/cities/drank/eaten，isolatedContext=GLM-ReadingInlinePreview-First-1）：preview 面板正确显示，cities 有 1 个 confirmed candidate 时「是这个意思/不是这个意思」按钮出现，点击后无新 POST/PUT/DELETE 请求，console 仅有 Vue 开发模式 + WebSocket 降级。**本轮不是阅读中评分；不写 ReviewLog；不改 FSRS；不真实调用 AI；不自动创建 WordSense/ReviewCard；不删除 legacy；不大规模 UI 重写；不新增 migration；800% 是子阶段提升，不是固定五条主线虚假上涨。** |
| GLM-ReadingInlineConfirmationPersistence-1000-1 | 阅读中词义确认持久化 + 产品冻结 ADR。新增 `docs/adr/ADR-0003-reading-inline-sense-confirmation-persistence.md` 产品冻结（确认结果只用于回显/统计/未来评分前置，不等于评分；不写 ReviewLog；不改 FSRS；不改 review_card due/reps/state；不自动创建 WordSense/ReviewCard；不真实调用 AI；occurrence 级别保存；「不是这个意思」只否定当前 occurrence 不全局否定；后续转评分必须另开 ADR；比较 A/B/C 三方案，选择 C：新增轻量 additive-only 表）。新增 migration `2026_07_03_000001_create_reading_inline_sense_confirmations_table.php`（additive-only，Schema::create only，唯一约束 user_id+language+chapter_id+sentence_index+surface+lemma+word_sense_id）；新增 `ReadingInlineSenseConfirmation` model（CHOICE_MATCH/CHOICE_NOT_MATCH/SOURCE_READING_INLINE_PREVIEW 常量）；新增 `ReadingInlineSenseConfirmationService`（唯一写入器，storeConfirmation() 用 DB::transaction + lockForUpdate 实现 upsert；listConfirmationsForOccurrence() 只读回显）；新增 `POST /senses/inline-confirmation` endpoint（验证 user/language/chapter/sense ownership + WordSense STATUS_CONFIRMED + choice 限定 match/not_match + 空 lemma/surface/sense 安全失败）；扩展 `GET /senses/inline-preview` 回显 persisted_choice/confirmation_id/confirmed_at/persisted_summary；改造 `InlineSensePreviewPanel.vue` 按钮持久化（POST + optimistic localOverride + updated_preview 替换 + 「已保存：是这个意思」/「已保存：不是这个意思」/「这不是复习评分」/「不会写入复习记录」/「不会改变 FSRS」文案 + 无 Good/Easy/Hard/Again + 无 rating route）。新增 19 个 confirmation guard tests（ReadingInlineSenseConfirmationTest）+ 6 个 echo tests（InlineSensePreviewTest 扩展）+ 14 个 UI guard tests（InlineSensePreviewUiGuardTest 重写）。MCP Chrome 真实验收 5 个 token（technology/published/substantive/endeavour/contested，isolatedContext=glm-inline-confirm-2026-07-03）：点击 technology 「是这个意思」保存成功 → 刷新页面回显「已保存：是这个意思」→ 点击「不是这个意思」更新为「已保存：不是这个意思」（DB upsert 同一记录）→ Network 仅有 POST /senses/inline-confirmation + GET /senses/inline-preview → 无 ReviewLog/FSRS/外部 AI 请求 → Console 仅 Vue dev + WebSocket 降级。**本轮不是阅读中评分；不写 ReviewLog；不改 FSRS；不真实调用 AI；不自动创建 WordSense/ReviewCard；不删除 legacy；不大规模 UI 重写；migration additive-only；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GLM-ReadingInlineConfirmationUsageSurface-AndMorphology-1000-1 | 阅读中确认结果使用层 + 随机新词 lemma 治理。新增 `ReadingInlineSenseConfirmationService::summaryForSenseCandidates()` 只读方法（聚合 match_count/not_match_count/last_choice/last_confirmed_at/recent_examples/has_any_confirmation，按 user+language 隔离，不写 ReviewLog/FSRS/WordSense/ReviewCard）；扩展 `GET /senses/inline-preview` payload 每个 candidate 附带 `usage_summary`/`usage_match_count`/`usage_not_match_count`/`usage_last_choice`/`usage_last_confirmed_at`；ADR-0003 新增 Usage Surface Layer 章节（8 条规则：confirmation 是阅读证据、可用于显示/统计、不等于评分、不影响 FSRS、不写 ReviewLog、not_match 不全局否定、FSRS 面向用户可改述为复习进度、后续转评分须另开 ADR）；改造 `InlineSensePreviewPanel.vue` 新增使用层显示块（「阅读中确认过 N 次」/「阅读中排除过 N 次」/「最近一次：是这个意思 / 不是这个意思」）+ `hasUsageSummary()` 模板门控 + 更友好安全文案（「不会改变复习进度（FSRS）」替代纯「不会改变 FSRS」）+ CSS 样式。新增 12 个 summary guard tests（ReadingInlineSenseConfirmationTest 扩展：聚合统计/用户隔离/语言隔离/严格只读/不写 ReviewLog/不改 FSRS/不创建 WordSense+ReviewCard/recent_examples 不泄漏/空 sense_ids/无确认零值条目/preview payload 含 summary/未知 lemma 空 summary）+ 4 个 UI guard tests（InlineSensePreviewUiGuardTest 扩展：使用层文案存在/全局否定文案不存在/安全文案更新）。R3 morphology 轮：新章节 15（book 10），10 个真实 Playwright 点击（pens/tables/cars/dogs/feet/teeth/ate/driven/smaller/closed），10/10 surface→lemma 正确（tokenizer 确认：feet→foot/teeth→tooth/ate→eat/driven→drive 等不规则形式 fallback 无法处理），0% 重复率 vs R0/R1/R2，8/8 类别覆盖。MCP Chrome 验收 match/not_match 交互流程（chapter 7 goose/geese：点击「是这个意思」→ POST 200 → UI 显示「已保存：是这个意思」→ 刷新 → 回显「已保存：是这个意思」→ 点击「不是这个意思」→ POST 200 → UI 显示「已保存：不是这个意思」→ DB upsert 同一记录），Network 仅 POST /senses/inline-confirmation + GET /senses/inline-preview，无 ReviewLog/FSRS/AI。**本轮不是阅读中评分；不写 ReviewLog；不改 FSRS；不真实调用 AI；不自动创建 WordSense/ReviewCard；不新增 migration；不删除 legacy；不大规模 UI 重写；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GLM-ReadingInlineConfirmationManagementSurface-1000-1 | 阅读中确认记录管理入口 + 随机新词 lemma 验收。新增 `ReadingInlineSenseConfirmationService::listConfirmationsForManagement()` 只读列表方法（按 user+language 隔离，支持 choice/lemma/surface/word_sense_id/chapter_id/date_from/date_to 筛选 + 分页，返回 confirmation_id/choice/surface/lemma/word_sense_id/WordSense 摘要/chapter_id/chapter_name/sentence_index/sentence_text/updated_at/source/can_revoke 字段）+ `revokeConfirmation()` 单条撤销方法（DELETE 操作，仅删除当前 user+language 自己的一条 `reading_inline_sense_confirmations` 记录，返回 `revoked:true` + 6 条 safety_flags：no_review_log_created/no_fsrs_changed/no_review_card_changed/no_word_sense_deleted/no_review_card_deleted/not_a_review_rating）；新增 `GET /senses/inline-confirmations` 列表端点（read-only，严格 user/language 隔离，不写数据）+ `DELETE /senses/inline-confirmations/{id}` 撤销端点（仅删单条 confirmation，不写 ReviewLog，不改 FSRS，不改 ReviewCard，不删 WordSense/ReviewCard/ReviewLog）；新增 `ReadingInlineConfirmationManage.vue` 管理页（`/senses/inline-confirmations/manage`：标题「阅读中词义确认记录」+ 说明「这些记录来自阅读页的"是这个意思/不是这个意思"，不是复习评分」+ 筛选 全部/是这个意思/不是这个意思/lemma/文章 + 列表 surface/lemma/choice/WordSense 摘要/来源句子/文章/最近更新时间 + 「查看来源」跳回阅读页 + 「撤销这条记录」操作 + 撤销确认弹窗「撤销这条阅读中确认记录？」+ 撤销后列表更新 + 空状态文案「暂无阅读中词义确认记录」+ 安全文案「这不是复习评分」/「不会写入复习记录」/「不会改变复习进度（FSRS）」/「不是忘记，不是复习失败，也不是删除词义」）；`InlineSensePreviewPanel.vue` 新增「查看全部阅读确认记录」链接到管理页。49 个后端护栏测试（list 端点 user/language 隔离、choice/lemma 筛选、WordSense 摘要、chapter/sentence 返回、revoke 仅删当前 user/language 单条、revoke 不能删其他用户/其他语言 confirmation、revoke 不写 ReviewLog/不改 FSRS/不删 WordSense/不删 ReviewCard、revoke 后 preview summary 更新、未知 id 安全失败、existing confirmation tests 全绿、ReviewFsrsTest 全绿、FsrsSchedulingServiceTest 全绿）+ 19 个 UI guard 测试（文件存在、标题文案、不是复习评分文案、不会写入复习记录文案、不会改变复习进度文案、是这个意思/不是这个意思文案、撤销这条记录文案、撤销弹窗、不是忘记/不是复习失败/不是删除词义文案、无 Good/Easy/Hard/Again、无 rating route、无 ReviewLog route、无 FSRS write route、无复习失败/忘记了/删除词义作为撤销含义、无批量撤销 UI、preview panel 链接到管理页、回到阅读页链接、空状态文案）。R4 morphology 轮：新章节 16（book 10），13 个真实 Playwright 点击（windows/horses/sheep/mice/deer/broke/drove/spoken/threw/grown/taller/walked/finished），11/13 surface→lemma 正确（tokenizer 经不规则形式 mice→mouse/broke→break/drove→drive/spoken→speak/threw→throw/grown→grow 确认可用，fallback 无法处理这些不规则形式），2 P2 residual 在 V-ed 歧义形式 walked/finished 与 R2 pattern 一致，0% 重复率 vs R3，8/8 类别覆盖。MCP Chrome 全管理流程验收（chapter 7 goose/geese：点击「是这个意思」→ POST 200 → UI 显示「已保存：是这个意思」→ 点击「查看全部阅读确认记录」→ 跳转 `/senses/inline-confirmations/manage` → 列表显示新 match 记录（surface=geese/lemma=goose/sentence=`The geese went to the lake.`/chapter=Test Sentences/choice=是这个意思）→ filter choice=match 显示该记录 → filter choice=not_match 隐藏该记录 → 点击「撤销这条记录」→ 确认弹窗 → 确认 → DELETE 200 返回 `revoked:true` + 6 safety_flags → 列表更新记录消失 → 回到 `/chapters/read/7` → 重新点击 geese → preview panel 不再显示「已保存：是这个意思」（persisted_choice 已清除）），Network 仅 POST /senses/inline-confirmation + GET /senses/inline-confirmations + DELETE /senses/inline-confirmations/{id}，无 ReviewLog/FSRS/AI。**本轮不是阅读中评分；不写 ReviewLog；不改 FSRS；不真实调用 AI；不自动创建 WordSense/ReviewCard；不删除 WordSense/ReviewCard/ReviewLog；不批量撤销；不实现评分按钮；不新增 migration；不删除 legacy；不大规模 UI 重写；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| OpenCode-ReadingInlineConfirmationUndoHotkey-800-1 | Anki-style Ctrl+Z 撤销最近一次阅读判断操作。新增 `ReadingInlineSenseConfirmationService::makeUndoTokenForStore()` / `makeUndoTokenForRevoke()` / `undoLastInlineConfirmationAction()` / `performStoreUndo()` / `performRevokeUndo()` 后端方法（Laravel `Crypt::encryptString()` 加密 JSON payload，含 v=1/user_id/language/action_type/confirmation_id/before_state/after_state/restore_payload/expires_at，120 秒过期，`DecryptException` 捕获转 422，user/language/WordSense STATUS_CONFIRMED/Chapter ownership 多重校验；store undo: before_state=null → DELETE，before_state=match/not_match → UPDATE 回 before_state；revoke undo: 重新校验后 `firstOrCreate` 重新插入行）；`storeConfirmation()` / `revokeConfirmation()` 修改为返回 `undo_token` / `undo_expires_at` / `undo_hint` 字段；`SenseOccurrenceController::storeInlineConfirmation` / `revokeInlineConfirmation` / 新增 `undoInlineConfirmation` 控制器方法；新增 `POST /senses/inline-confirmations/undo` 路由（必须在 `DELETE /senses/inline-confirmations/{id}` 之前注册避免 `{id}` 拦截 `undo`）；`InlineSensePreviewPanel.vue` 阅读页新增 undoToken/undoSnackbar/undoLoading data + mounted/beforeDestroy keydown listener + handleKeyDown/isFocusInsideEditableInput/triggerUndo methods + axios POST `/senses/inline-confirmations/undo` + 「按 Ctrl+Z 可撤销刚才的阅读判断。」snackbar；`ReadingInlineConfirmationManage.vue` 管理页撤销按钮文案从「撤销这条记录」改为「撤销这条阅读判断」+ 同上 keydown listener + undo 后 reload 列表 + 「按 Ctrl+Z 可恢复。」snackbar；ADR-0003 新增「Undo Hotkey Layer」章节（12 条规则：撤销最近一次阅读判断、不是评分、不是 Forget、不是 Again、不是删除词义、支持 store undo、支持 choice-switch undo、支持 revoke undo、不做 Redo、不做多级 undo、不拦截 input 内 Ctrl+Z）。17 个新增安全护栏测试（store/revoke 返回 undo_token、store from none undo 删除刚创建 confirmation、store from match→not_match undo 恢复 match、store from not_match→match undo 恢复 not_match、revoke undo 恢复删除 confirmation、invalid/expired/cross-user/cross-language token 拒绝、WordSense/Chapter 所有权变更后 undo revoke 拒绝、undo 不写 ReviewLog、undo 不改 FSRS、undo 不创建 WordSense/ReviewCard、undo 不删除 WordSense/ReviewCard/ReviewLog）+ 8 个新增/更新 UI guard 测试（管理页 undo hint 文案、keydown listener 注册/移除、input guard、undo endpoint 安全调用、token 用后清空、阅读页 undo hint 文案、阅读页 keydown+input guard、阅读页 undo endpoint 安全调用）。R5 morphology 轮：6 个真实 MCP Chrome 点击（geese→goose/better→good/best→good/went→go/children→child/ran→run），3/8 类别覆盖（regression 验证轮，非新 8/8 sample 轮）。MCP Chrome 全 undo 流程验收：阅读页 store undo（点击「是这个意思」→ Ctrl+Z →「已保存」消失）✓、阅读页 choice-switch undo（切换「不是这个意思」→ Ctrl+Z → 恢复「是这个意思」）✓、管理页 revoke undo（点击「撤销这条阅读判断」→ 确认 → 列表 2→1 条 + 「按 Ctrl+Z 可恢复」snackbar → Ctrl+Z → 列表 1→2 条恢复）✓、input 内 Ctrl+Z 不拦截（textbox 输入「goose」→ Ctrl+Z → 浏览器原生 undo 清空，无业务 endpoint 调用）✓、Network 仅 POST /senses/inline-confirmation + POST /senses/inline-confirmations/undo + DELETE /senses/inline-confirmations/{id}，无 ReviewLog/FSRS/AI ✓、Console 仅预期 WebSocket 降级 ✓。**本轮不是阅读中评分；不写 ReviewLog；不改 FSRS；不真实调用 AI；不自动创建 WordSense/ReviewCard；不删除 WordSense/ReviewCard/ReviewLog；不新增 migration；不删除 legacy；不大规模 UI 重写；800% 是 8 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GLM-SenseReviewRealSmoke-1000-1 | SenseReview 词义复习主线真实刷卡 smoke 与每日上限入口最小修复。MCP Chrome 真实浏览器在 `/reviews/senses` 刷 5 张 sense cards（card_id 57/69/64/67/70，覆盖 good/hard/again/easy/good），验证查看答案、评分按钮、Space/1/2/3/4 快捷键（含临时卡 card_id=72 marker=glm_hotkey3_smoke_20260705_082121 验证快捷键 3=Good，验收后已清理）、More 菜单 5 项（查看原文/编辑/归档/重置/彻底删除）、查看原文对话框（非 fallback，已定位到原文位置）、空状态文案。5 条 ReviewLog（log_id 9-13）全部 `source=sense_review` 无重复写入，5 个 WordSense 保持 confirmed，confirmed_word_senses=13 一致，user/language 隔离正确。发现 P1：`SenseReview.vue` 前端只使用 `summary.due_count`，不显示 `limit_message` 和超额入口，导致达到每日上限后用户无法继续复习隐藏的到期卡。最小修复（仅 `SenseReview.vue`）：新增 `ignoreDailyLimits` data 状态；`loadCards()` / `rate()` 方法支持传递 `ignoreDailyLimits` 参数；统计区新增 `limit_message` info alert + 「继续复习超额卡片」按钮（基于 `summary.can_continue_over_limit`）+ `ignoreDailyLimits` 模式下的 warning alert + 「恢复上限」按钮；新增 `continueOverLimit()` / `restoreLimits()` 方法。MCP Chrome 真实验收 P1 修复：临时构造 daily_review_limit=1 + card_id=56 重新到期场景（marker=`__VG_SENSE_REVIEW_SMOKE__`），验证 limit_message 显示 + 「继续复习超额卡片」→ `GET /reviews/senses?ignoreDailyLimits=true` 返回 1 张卡 + 「恢复上限」→ `GET /reviews/senses` 返回 0 张卡 + limit_message 重新显示，Network 仅 3 个 GET 请求，Console 仅预期 WebSocket 降级，验证后立即恢复临时数据。自动测试：SenseReview 19 passed / ReviewCardManageDangerContractTest 13 passed / WordSense 193 passed / ReviewCard 288 passed / FsrsSchedulingServiceTest passed / TestingDatabaseHealthConfigTest+TestingDatabaseHealthTest passed / npm run development 编译成功；ReviewFsrsTest 1 个预存在失败（legacy `/reviews` word review 端点测试，不是 sense review 端点，不在本轮范围）。smoke playbook 新增 Hotkey Acceptance + Daily Limit Acceptance 章节。**不改 FSRS 算法 / ReviewLog 逻辑 / AI / inline confirmation / ReviewCardManage / Source Context fuzzy；不新增 migration；不自动进入下一任务；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GLM-EnglishLemmaPhilosophyText-1000-1 | 哲学文本英文词元归并修复与旧数据安全修复闭环。根因：实时 Python tokenizer 已正确（construed→construe 全 10/10），问题在旧数据（B 类：PHP fallback 时代 lemma=construed 未修复）。修复：扩展 `tools/tokenizer.py` 健康检查覆盖 10 个哲学文本词 + 4 个 guard 词；扩展 `TokenizerDoctor` 显示哲学文本结果；修复 `StudyBaseDoctor` P1 bug（ECDICT 含 bogus stems 导致 this→thi/breed→bre 等误判，新增 PROTECTED_BASE_WORDS/BAD_STEMS 守卫 + 阈值提高 + -ed 自词元词修复 lemma/base_word 同步）；新增 `StudyBaseDoctorComputeLemmaTest` 13 个单元测试锁定 P1 修复；扩展 `TokenizerDoctorTest` 4 个 JSON 解析测试；旧数据修复：`php artisan study-base:doctor --fix` 修复 65 个高置信词的 study_base/lemma/base_word。MCP Chrome 验收：阅读页点击 construed → 侧栏显示 `construed → construe [修改]` → Network 所有 sense 查询使用 `lemma=construe`。不改 FSRS / ReviewLog / AI / Source Context / ReviewCardManage / inline confirmation / migration；不删除 WordSense/ReviewCard/ReviewLog；不覆盖用户 study_base override；不伪造 MCP Chrome。 |
| GLM-ReviewPayloadSourceOwnershipHardening-1000-1 | Review payload 来源所有权护栏修复。修复 `ReviewFsrsTest::test_sense_review_sentence_tokens_do_not_cross_user_scope` 暴露的跨用户来源句泄漏 P1 风险。根因：`WordSenseExamplePoolService::exampleCandidates()` 在 batch-loaded chapters 按 user_id+language 过滤后找不到跨用户/跨语言 chapter 时，仍然把 `occurrence.sentence_en` 加入 candidates，导致 `SenseReviewCardSerializerService::serialize()` 第 50 行用不可信 occurrence 的 sentence_en 作为题面 `example_sentence_en`，同时 `example_candidates` 数组也直接返回给前端。最小修复（仅 `app/Services/WordSenseExamplePoolService.php`）：在遍历 occurrences 时新增 ownership guard — 当 `occurrence.chapter_id !== null && !isset($chaptersById[$occurrence->chapter_id])` 时 `continue` 跳过该 occurrence，不加入 candidates，不进入题面/补充例句/synthetic fallback。fallback 仍走 `WordSense.example_sentence_en`（card_example candidate）。`SenseTokenPayloadService` 经审查安全（Layer 1/2 chapter 查询已按 user_id+language 过滤，Layer 3 synthetic 用 `$sense->example_sentence_en`），无需修改。`SenseSourceContextService::sourceContext/sourceContextList` 经侦察安全（sourceContextList 第 141-144 行 `$chapter` 为 null 时 continue 跳过；sourceContext 用 `resolver->findChapterById($chapterId, $sense->user_id, $sense->language_id)` 过滤），无需修改。新增 7 个 contract tests（WordSenseExamplePoolTest：跨用户 chapter 跳过 + 跨语言 chapter 跳过 + invalid chapter_id 跳过 + chapterless occurrence 保留为 occurrence-only + supplementary 不来自不可信 occurrence；ReviewFsrsTest：`/reviews/senses` 端点跨用户不泄漏 + `/reviews` 端点 fallback 到 WordSense.example_sentence_en 且 token_source=synthetic）+ 修改 1 个现有测试（`test_pool_chapter_title_resolves_null_when_chapter_belongs_to_other_user` 从期望保留 foreign occurrence 改为期望跳过，收紧安全边界而非降低断言强度）。`/reviews` 与 `/reviews/senses` 双入口均覆盖（共用 `SenseReviewCardSerializerService::serialize`）。自动测试：TestingDatabaseHealthConfigTest 6 passed / TestingDatabaseHealthTest 6 passed / ReviewFsrsTest 63 passed（从 1 failed 变为全绿）/ SenseReview 19 passed / WordSenseExamplePoolTest 26 passed / ReviewCard 288 passed / ReviewLog 1 passed / WordSense 197 passed / ReviewCardManageDangerContractTest 13 passed / npm run development 编译成功。MCP Chrome `/reviews/senses` smoke：页面正常显示空状态，Network 仅 3 个 GET（reviews/senses + review-cards/stats + fonts），Console 仅预期 WebSocket 降级。**不改 FSRS 算法 / ReviewLog 写入 / ReviewCardManage / inline confirmation / AI / tokenizer / migration；不自动进入下一任务；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GLM-AIStudyCardManualCandidatesToSenseReviewCard-1000-1 | AI 示意卡 V5 用户确认生成学习卡闭环。把 AI 示意卡从"最终候选包"推进到"用户确认后真正生成学习卡"的最小闭环。新增 `POST /ai-study-card/generate-cards` 后端接口（接收用户确认的最终候选项 + final_candidates_package，三重隔离 user/language/pending item，批量上限 50 项，每项独立事务：校验 → createOrFindSense → ensureSenseCard → createManualOccurrence，返回 created/skipped/duplicate/failed 四类结果 + summary + safety_flags 6 条：no_ai_called_by_linguacafe/ai_response_pasted_by_user/no_review_log_written/no_fsrs_rescheduled/no_legacy_word_card_created/user_confirmation_received）；在 `AiStudyCardPendingItemService` 添加构造函数注入 `WordSenseService` + `generateCardsFromConfirmedCandidates()` 方法约 270 行；在 `AiStudyCardPendingItemController::generateCards()` 方法做请求验证；在 `VocabularySideBox.vue` 添加 V5 完整 UI（生成学习卡按钮 + 确认生成对话框 + 每个候选项中文释义 v-text-field 必填 + 生成结果展示区域：created/skipped/duplicate/failed 四类 chip 统计 + 新建学习卡列表 + 「进入 /reviews/senses 复习」按钮 + 「这不是 AI 自动调用，是你粘贴 AI 返回内容后的人工确认生成」安全文案）。新增 13 个 V5 feature tests（AiStudyCardPendingItemTest：creates confirmed WordSense / creates target_type=sense ReviewCard / does not create legacy word ReviewCard / does not write ReviewLog / does not reschedule existing FSRS cards / unselected items not generated / deduplicates duplicate candidates / rejects cross-user pending item / rejects cross-language pending item / safely fails on invalid chapter or pending item / rejects empty sense_zh / rejects oversized batch / returns frontend displayable payload）。自动测试：AiStudyCardPendingItemTest 69 passed (384 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealth 12 passed (97 assertions) / npm run development 编译成功。MCP Chrome 真实全流程验收：登录 1816529781@qq.com → 阅读页 /chapters/read/16 → 点击 windows → 加入「待 AI 解释」（POST /ai-study-card/pending-items 200 item_id=4）→ 打开「待 AI 解释列表」→ 「生成 AI 示意卡」→ 「准备生成」预览包（POST /preview-package 200 schema_version=ai-study-card-preview-package-v1）→ 粘贴 AI 推荐词 JSON（agency/observation 2 条）→ 「解析推荐词」→ 勾选 agency → 「生成最终候选包」（POST /final-candidates-package 200 schema_version=ai-study-card-final-candidates-v1 user_selected=1 ai_recommended_selected=1 ai_recommended_unselected=1）→ 「生成学习卡」→ 确认对话框填写中文释义（windows=窗户；窗口 agency=代理；机构；力量）→ 「确认生成学习卡」（POST /generate-cards 200 success=true created=2 skipped=0 duplicate=0 failed=0）→ 验证结果展示 → 导航 /reviews/senses → 8 张到期卡包含新 card_id=73 (window 窗户；窗口) + card_id=74 (agency 代理；机构；力量) fsrs_state=new fsrs_due_at=now。数据库事实：WordSense 25→27 (+2 confirmed) / ReviewCard 25→27 (sense 19→21, word 6→6 unchanged) / ReviewLog 13→13 (unchanged) / 已有卡 FSRS 调度未重排。Network 全部 127.0.0.1:8000，无外部 AI 调用。Console 仅预期 WebSocket 降级。**不自动调 AI；不改 .env；不新增 API key；不接入 AI provider；不写 ReviewLog；不直接打 FSRS 分数；不改 FSRS 算法；不重排已有卡；不改已有 ReviewCard 调度数据；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy word card 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作；不新增 migration；不清库；不 notification script；不 DCP；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |

| GM52-AIStudyCardV5-HardeningAndSourceBinding-1000-1 | AI 示意卡 V5 安全收口与来源绑定。对 V5「用户确认最终候选后生成学习卡」进行 hardening：修正 6 个问题。问题 1：ai_recommended confirmed item 缺少后端反向校验 → 新增 `packageDedupeKey()` 辅助方法，从 final_candidates_package 提取 user_selected item_id 集合 + ai_recommended 安全 key 集合，confirmed_items 中 source=ai_recommended 必须能在 final package.ai_recommended_selected_items 中找到对应 dedupe key，user_selected 必须 item_id + word/lemma 双重匹配。问题 2：中文释义必填，英文解释可空 → 后端 validation `sense_zh` required / `sense_en` nullable，新增 `normalizeNullableString()` 将空字符串转 null，前端对话框 sense_en 非必填。问题 3：AI reason 不能自动变成中文释义 → 前端 `openGenerateCardsDialog()` 中 ai_recommended item 的 `reason` 只作为参考说明展示（带灯泡图标灰色背景），`sense_zh` 默认为空要求用户手动填写，placeholder 明确「推荐理由不是释义，请填写中文释义」。问题 4：来源例句/occurrence 绑定收口 → 新增 synthetic sentence_id 生成（格式 `ai-study-card:{chapter_id}:{text_block_index}:{sentence_index}:{normalized_word}`），4 种情况：explicit sentence_id 直接写 occurrence / 无 sentence_id 但有 chapter_id+sentence_text+text_block_index 写 synthetic occurrence / 无足够来源信息不写 occurrence 但创建卡 / 跨用户跨语言 chapter 跳过。问题 5：结果 payload 更清楚 → 每个 created/skipped/duplicate/failed item 含 word/lemma/sense_id/review_card_id/occurrence_created/occurrence_id/source_binding_status/source_binding_reason，前端展示 source_binding_status chip。问题 6：文档事实对齐 → 更新 current-working-handoff.md 修正旧说法（"下一阶段不应直接生成复习卡"改为"V5 已实现最小生成闭环，下一步是 V5 hardening / V6 前置设计"）。修改文件：`app/Services/AiStudyCardPendingItemService.php`（重写 generateCardsFromConfirmedCandidates 约 330 行 + 4 个新辅助方法）、`resources/js/components/Text/VocabularySideBox.vue`（reason 不填 sense_zh + sense_en 可选 + UI 增强 + 来源绑定状态展示）、`tests/Feature/AiStudyCardPendingItemTest.php`（17 项 V5 hardening 测试 + 辅助方法修改）。自动测试：AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions) / npm run development 编译成功。MCP Chrome 真实全流程验收：登录 → /chapters/read/6 → 点击 construed → 待 AI 解释（POST /pending-items 200 item_id=5）→ 列表 → preview-package → 粘贴 AI 推荐词 JSON（phenomenological + modernization 2 条，reason 是推荐理由）→ 勾选 2 条 → final-candidates-package → 生成学习卡对话框（验证 AI reason 不自动填 sense_zh，3 个 sense_zh 为空）→ 填写中文释义（construed=理解为；被解释为 / phenomenological=现象学的 / modernization=现代化，英文解释全留空）→ 确认生成（POST /generate-cards 200 success=true created=3 skipped=0 duplicate=0 failed=0）→ 结果：construed occurrence_created=true source_binding_status=来源已绑定（合成 sentence_id）occurrence_id=22 sentence_id=ai-study-card:6:0:0:construed / phenomenological occurrence_created=false source_binding_status=来源信息不足 reason=no_chapter_id / modernization 同上。数据库事实：WordSense 27→30 (+3 confirmed) / ReviewCard 27→30 (sense 21→24, word 6→6 unchanged) / WordSenseOccurrence 21→22 (+1 synthetic) / ReviewLog 13→13 (unchanged) / 已有卡 FSRS 未重排（card#56/57/58 reps/stability/due 不变）。Network 全部 127.0.0.1:8000，无外部 AI 调用。Console 仅预期 WebSocket 降级。**不自动调 AI；不改 .env；不新增 API key；不接入 AI provider；不写 ReviewLog；不改 FSRS；不重排已有卡；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；1000% 是 9 个子阶段合计提升，不是固定五条主线虚假上涨。** |
| GM52-AIStudyCardV5-DesktopArchitectureConvergence-1000-1 | 桌面端 AIStudyCard V5 架构收敛。把 `VocabularySideBox.vue`（宽屏右侧栏）与 `VocabularyBox.vue`（电脑半屏/窄窗口浮动框）中重复的 V5 生成学习卡逻辑抽成共享结构，让两个电脑端入口复用同一套 V5 组件/helper/service。本轮纠正上一轮网页端总流程设计师把 `VocabularyBottomSheet.vue` 当作"手机端入口"的范围误判：当前项目只支持电脑端，不做手机端，不补 BottomSheet 的 V5 流程，只处理宽屏 SideBox 与半屏 VocabularyBox 两个真实入口。新增 3 个共享文件：`resources/js/services/AiStudyCardGenerateCardsService.js`（3 个纯函数 `buildGenerateCardItems` / `filterConfirmedGenerateCardItems` / `generateAiStudyCards`，纯前端逻辑不写后端/DB/ReviewLog/FSRS，AI reason 只作参考说明不写入 sense_zh，user_selected 保留 chapter_id/text_block_index/sentence_index/sentence_text，ai_recommended 默认无 chapter_id 保持当前行为，sense_zh 必填初始化为空，sense_en 初始化为空允许为空）、`resources/js/components/Text/AiStudyCardGenerateCardsDialog.vue`（共享确认对话框组件：v-model + items/loading/error props + confirm event；展示候选词/lemma/source chip/AI reason 参考说明；中文释义必填，英文解释可选；"共 N 项，已填 M 项"计数；不直接请求后端，不感知父组件存在）、`resources/js/components/Text/AiStudyCardGenerateCardsResult.vue`（共享结果展示组件：result prop + go-to-sense-reviews/dismiss events；展示 created/skipped/duplicate/failed 四类结果 + sense_id/review_card_id + source_binding_status + "进入 /reviews/senses 复习"入口 + "这不是 AI 自动调用，是你粘贴 AI 返回内容后的人工确认生成"安全文案；不直接请求后端，不写 ReviewLog/FSRS/ReviewCard）。SideBox 与 VocabularyBox 接入方式：保留原有入口位置与视觉文案，删除重复的 V5 确认弹窗模板/结果展示模板/buildGenerateCardItems 构造逻辑/confirmGenerateCards 请求逻辑，改为：父组件只持有 dialog open 状态 + result/loading/error + 调用共享 helper + 把数据传给共享组件 + 跳转 /reviews/senses。新增 `tests/Feature/AiStudyCardV5DesktopArchitectureGuardTest.php`（17 项测试，覆盖共享文件存在性 / 两个父组件引用共享组件 / 不再包含大段重复 V5 模板（指纹检测：`推荐理由（参考说明，不是释义）：{{ item.reason }}` / `<v-dialog v-model="aiGenerateCardsDialog"` / `新建学习卡：` / `生成学习卡结果` / `source: 'user_selected',`）/ 不再重复 buildGenerateCardItems 逻辑 / AI reason 不自动填 sense_zh / sense_zh 初始化为空 / sense_en 允许为空 / VocabularyBottomSheet.vue 不含 V5 流程 / 无外部 AI provider 字符串 / 无 ReviewLog/FSRS rating/legacy word card 创建调用 / Service 暴露 3 个纯函数 / Dialog/Result 只 emit 事件不直接调后端）；重写 `tests/Feature/VocabularyBoxV5UiGuardTest.php`（16 项测试，扫描目标从单一 VocabularyBox.vue 扩展到 4 个文件）。自动测试 9 个套件全绿（363 tests / 0 failures）：AiStudyCardV5DesktopArchitectureGuardTest 17 passed (134 assertions) / VocabularyBoxV5UiGuardTest 16 passed (104 assertions) / AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)；`npm run development` 编译成功（5663ms，7.37 MiB app.js）。MCP Chrome 真实双 viewport 回归验收全通过：A. 宽屏 1920x900 `/chapters/read/6` 点击 construed → VocabularySideBox → 完整 V5 流程（待 AI 解释 → 列表 → preview-package → 粘贴 AI 推荐词 JSON → final-candidates-package → 生成学习卡 → 共享 Dialog 渲染正确：安全文案 + sense_zh 必填 + sense_en 可选 + 计数显示 → 填写中文释义 → 确认生成 → POST /generate-cards 200 → 结果显示 0 created, 1 duplicate (释义 #73), source_binding_status=来源已绑定（合成 sentence_id）→ 进入 /reviews/senses → Network 全部 127.0.0.1:8000 无外部 AI 调用 → Console 仅 WebSocket 降级（pre-existing, non-blocking））；B. 电脑半屏 900x900 `/chapters/read/6` 点击 construed → VocabularyBox → 同样完整 V5 流程，共享 Dialog/Result 均正常工作 → POST /generate-cards 200 → 结果 0 created, 1 duplicate → Network 全部本地 → Console 仅 WebSocket 降级。**不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；保持 UI 文案和用户流程不变；不自动调 AI；不接 AI provider；不读写 .env；不新增 API key；不写 ReviewLog；不改 FSRS；不重排已有卡；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作逻辑；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收；不改 TextBlockGroup.vue；不实现手机端/BottomSheet V5；下一步仍由网页端总流程设计师决定，不自动进入 V6。** |
| GM52-AIStudyCardV5-SideBoxMojibakeRegressionFix-1000-3 | VocabularySideBox.vue 中文 mojibake P1 回归修复。上一轮 feature island 抽取（commit 39fd175）虽然修复了 mojibake 引起的语法错误（破损标签、缺失引号），但没有修复用户可见中文文案，反而引入了宽屏右侧栏中文乱码（27 处 mojibake，含 PUA 字符 U+E000-U+F8FF，例如 `请选择一个单词或短语` 被破坏成 `璇烽€夋嫨涓€涓\ue044崟璇嶆垨鐭\ue161\ue1e2`）。本轮基于 `e53d116` 版本的 `VocabularySideBox.vue` 作为中文文案基准，通过 Python 脚本（项目有 Python tokenizer 服务，避免 PowerShell 5.x 读取 UTF-8 无 BOM 文件的编码问题）逐处恢复 27 处 mojibake：第 24 行 `请选择一个单词或短语` / 第 29 行 `新短语` / 第 30/43 行 `单词` / 第 30/101/193/194 行 `短语`（含 PUA 字符） / 第 32 行 `显示变形` / 第 33 行 `朗读`（含 PUA 字符） / 第 34 行 `发送到 Anki` / 第 35 行 `返回单词` / 第 36 行 `取消选择` / 第 43 行 `单词基础信息`（含 PUA 字符） / 第 52/56 行 `[修改]`（含 PUA 字符） / 第 77 行 `发音` / 第 80/91 行 `FSRS 熟悉度：尚未复习` / 第 96 行 `词元读音` / 第 98/102 行 `读音` / 第 106 行 `忽略` / 第 107 行 `标为已知` / 第 108 行 `回归为新词` / 第 109 行注释 `由共享 feature island 组件负责` / 第 129/133 行 `添加新释义` / 第 139 行 `条建议` / 第 148 行 `搜索词典...` / 第 161 行 `候选结果（AI + 词典）` / 第 185 行 `手动输入释义` / 第 193 行 `保存短语`（含 PUA 字符） / 第 194 行 `删除短语`（含 PUA 字符） / 第 200 行 `形式` `肯定` `否定` / 第 448 行 `无法读取 AI 建议。`（含 PUA 字符）。修复策略：以 `e53d116` 为中文文案基准手动恢复，**不**使用 `git checkout e53d116 -- VocabularySideBox.vue` 覆盖当前文件（那会把 feature island 改回旧重复实现），**不**回滚整个 commit 39fd175，保留 `import AiStudyCardDesktopWorkflow` / components 注册 / `<AiStudyCardDesktopWorkflow ref="aiStudyCardWorkflow" />` 挂载，不恢复旧 V1-V5 重复模板。新增 `tests/Feature/VocabularySideBoxChineseTextIntegrityTest.php`（5 项测试 / 42 assertions）：覆盖文件存在性 / 包含 21 条核心正常中文文案 / 不包含 12 条已知 mojibake 片段（`璇烽` / `鍗曡瘝` / `鐭` / `鏂扮` / `淇` / `鏈楄` / `鍙戦` / `杩斿` / `FSRS 鐔` / `?/span` / `?/v-btn` / `?/v-chip`）/ 仍挂载 `AiStudyCardDesktopWorkflow`（证明 feature island 未回滚）/ 不包含旧重复 workflow 指纹（`<v-dialog v-model="aiPendingListDialog"` / `<v-dialog v-model="aiStudyCardPreviewDialog"` / `aiPreviewPackage: null` / `parseAiRecommendations()` / `generateFinalCandidatesPackage()`）。自动测试 12 个套件全绿（390 tests / 0 failures）：VocabularySideBoxChineseTextIntegrityTest 5 passed (42 assertions) / AiStudyCardDesktopWorkflowArchitectureGuardTest 22 passed (293 assertions) / AiStudyCardV5DesktopArchitectureGuardTest 17 passed (146 assertions) / VocabularyBoxV5UiGuardTest 16 passed (131 assertions) / AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)；`npm run development` 编译成功（5493ms，7.35 MiB app.js）。MCP Chrome 真实双 viewport 中文文案验收全通过：A. 宽屏 1920x900 `/chapters/read/6` 点击 construed → VocabularySideBox 显示正常中文（`单词` / `发送到 Anki` / `取消选择` / `单词基础信息` / `[修改]` / `忽略` / `标为已知` / `回归为新词` / `添加新释义` 全部正常，无 mojibake）→ 点击「待 AI 解释列表」→ 弹窗显示「待 AI 解释的词 / 待解释 (1) / 已取消 (0) / 取消 / 生成 AI 示意卡 / 当前共 1 个待解释词」全部正常中文 → Network 27 个请求全部 127.0.0.1:8000 → Console 仅预期 WebSocket 降级；B. 半屏 900x900 `/chapters/read/6` 点击 construed → VocabularyBox 显示正常中文（`忽略` / `标为已知` / `回归为新词` / `待 AI 解释` / `待 AI 解释列表` / `词典搜索` / `+ 添加新释义` / `当前词形` / `词元：` / `候选预览` / `是这个意思` / `不是这个意思` / `已学词义候选` / `已学` / `已复习 0 次` / `编辑该释义` / `删除释义` / `下次复习：今天 状态：新卡` / `查看例句` / `关闭` / `编辑` / `发送到 Anki` 全部正常，无 mojibake）→ Network 35 个请求全部 127.0.0.1:8000 → Console 仅预期 WebSocket 降级。反驳式侦查结论：上一轮「修复 mojibake」说法**不完整** — 只修复了语法错误（破损标签/缺失引号），没有修复用户可见中文文案；本轮真正修复了所有用户可见中文。**不新增产品功能；不回滚 feature island；保留 AiStudyCardDesktopWorkflow；不恢复旧 V1-V5 重复模板；不修改后端；不修改 VocabularyBox.vue（侦查确认无 mojibake）；不修改 AiStudyCardDesktopWorkflow.vue（侦查确认无 mojibake）；不自动调 AI；不接 AI provider；不读写 .env；不新增 API key；不写 ReviewLog；不改 FSRS；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收；下一步仍由网页端总流程设计师决定，不自动进入 V6 或 workflow 再拆分。** |
| GM52-AIStudyCardV5-DesktopWorkflowFeatureIsland-1000-2 | 桌面端 AIStudyCard V1-V5 工作流收敛为独立 feature island。在上一轮（DesktopArchitectureConvergence）只把 V5 生成学习卡的 Dialog/Result/Service 收敛的基础上，本轮把 V1（pending item 标记）→ V2（待解释列表/取消/恢复）→ V3（preview-package 安全包）→ V4（粘贴 AI 推荐词 JSON / parse / dedupe / final-candidates-package）→ V5（generate-cards 确认生成学习卡）的完整桌面端流程收敛到一个独立 feature island 组件 `AiStudyCardDesktopWorkflow.vue`，让 `VocabularySideBox.vue`（宽屏右侧栏）与 `VocabularyBox.vue`（电脑半屏/窄窗口浮动框）只负责挂载该组件，不再各自维护完整 AIStudyCard 流程。新增 3 个文件：`resources/js/components/Text/AiStudyCardDesktopWorkflow.vue`（约 1019 行 feature island 组件，包含 V1-V5 全部模板/data/methods：`待 AI 解释` 按钮、`待 AI 解释列表` 按钮、待解释/已取消视图切换 + 列表/取消/恢复、preview-package 预览弹窗 + 全选/全不选 + 用户已选词列表 + AI 推荐词粘贴文本框 + 解析/清空 + AI 推荐词列表默认 unchecked + 全选/全不选 + 「生成最终候选包」+ 生成学习卡对话框 + 结果展示 + 「进入 /reviews/senses 复习」入口；通过 `computed: mapState('vocabularyBox', { _chapterId/_sentenceIndex/_sentenceText/_studyBase/_baseWord })` 从 store 获取上下文，不通过 props 传递上下文）、`resources/js/services/AiStudyCardRecommendationParserService.js`（225 行纯函数模块，导出 `buildUserSelectedKeys(finalCandidatesPackage)` / `parseAiRecommendations(rawJson, userSelectedKeys)` / `rededupeRecommendations(parsed)`；不 import Vue/Vuex/DOM；解析失败返回 `{ ok: false, error, recommendations: [] }`）、`resources/js/services/AiStudyCardPendingWorkflowService.js`（195 行 API 封装模块，导出 `createPendingItem(axios, payload)` / `listPendingItems(axios, params)` / `dismissPendingItem(axios, id)` / `restorePendingItem(axios, id)` / `buildPreviewPackage(axios, payload)` / `buildFinalCandidatesPackage(axios, payload)`；re-export `generateAiStudyCards` / `buildGenerateCardItems` / `filterConfirmedGenerateCardItems` from `AiStudyCardGenerateCardsService.js`）。修改 `VocabularySideBox.vue`：删除 V1-V5 完整模板/data/methods，改为引入并挂载 `AiStudyCardDesktopWorkflow`。修改 `VocabularyBox.vue`：同 SideBox 删除并挂载 `AiStudyCardDesktopWorkflow`。新增 `tests/Feature/AiStudyCardDesktopWorkflowArchitectureGuardTest.php`（22 项测试 / 293 assertions）：覆盖 feature island 文件存在性 / 两个父组件挂载该组件 / 不再包含重复 V1-V5 模板与 data 字段（指纹检测：`待 AI 解释`、`<v-dialog v-model="aiPendingListDialog"`、`<v-dialog v-model="aiStudyCardPreviewDialog"`、`aiPendingItems`、`aiPreviewPackage`、`aiFinalCandidatesPackage`、`aiSelectedRecommendationIndices`）/ 父组件只持有必要最小状态 / `AiStudyCardRecommendationParserService` 暴露 3 个纯函数 / `AiStudyCardPendingWorkflowService` 暴露 6 个函数 + re-export 3 个 / 解析失败安全失败 / `AiStudyCardGenerateCardsDialog` 与 `AiStudyCardGenerateCardsResult` 仍由上一轮 V5 收敛保持不变 / AI 推荐词默认 unchecked / AI reason 不自动填 sense_zh / sense_zh 初始化为空 / sense_en 允许为空 / `VocabularyBottomSheet.vue` 不含 V1-V5 流程 / 无外部 AI provider 字符串 / 无 ReviewLog/FSRS rating/legacy word card 创建调用。自动测试 10 个套件全绿（385 tests / 0 failures）：AiStudyCardDesktopWorkflowArchitectureGuardTest 22 passed (293 assertions) / AiStudyCardV5DesktopArchitectureGuardTest 17 passed (146 assertions) / VocabularyBoxV5UiGuardTest 16 passed (131 assertions) / AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)；`npm run development` 编译成功。MCP Chrome 真实双 viewport 完整 V1-V5 回归验收全通过：A. 宽屏 1920x900 `/chapters/read/6` 点击 construed → VocabularySideBox → 完整 V1-V5 流程（POST /pending-items 200 item_id=5 → 列表显示「待解释 (1)」→ dismiss → 「已取消 (1)」→ restore → 「待解释 (1)」→ preview-package 200 safety_flags 全 true → 粘贴 AI 推荐词 JSON（agency/observation 2 条）→ 解析成功，2 条默认 unchecked，勾选 agency → final-candidates-package 200 → 生成学习卡对话框 → 填写 sense_zh → POST /generate-cards 200 → 「已生成 2 张学习卡」→ 数据库 WordSense #78/79 + ReviewCard #80/81 (target_type=sense) + 0 ReviewLog + 0 legacy word card → Network 30 个请求全部 127.0.0.1:8000 → Console 仅 WebSocket 降级）；B. 半屏 900x900 `/chapters/read/6` 点击 construed → VocabularyBox → 导航栏折叠为「更多」菜单（半屏行为正确）→ 完整 V1-V5 流程，使用不同 AI 推荐词（progress）→ POST /generate-cards 200 → 「已生成 2 张学习卡」→ 数据库 WordSense #80/81 + ReviewCard #82/83 (target_type=sense) → Network 19 个请求全部 127.0.0.1:8000。关键安全契约两个 viewport 均确认：AI 推荐词默认 unchecked ✓ / AI reason 不自动填 sense_zh ✓ / sense_zh 初始化为空 ✓ / sense_en 允许为空（实际为 null）✓ / 无外部 AI provider 调用 ✓ / 无 ReviewLog 写入 ✓ / 无 legacy word card 创建 ✓ / 所有 ReviewCard target_type=sense ✓。**不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；保持 UI 文案和用户流程不变；不自动调 AI；不接 AI provider；不读写 .env；不新增 API key；不写 ReviewLog；不改 FSRS；不重排已有卡；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作逻辑；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收；不改 TextBlockGroup.vue；不实现手机端/BottomSheet V1-V5；下一步仍由网页端总流程设计师决定，不自动进入 V6。** |

| GM52-AIStudyCardPendingLifecycleClosure-1000-5 | AI 示意卡 pending item 生命周期收口。本轮同轮完成检测 + 编码，正式冻结 pending item 生命周期规则：user_selected + created/duplicate → pending item 标记 processed；user_selected + skipped/failed → 保持 pending；ai_recommended → 不修改任何 pending item；dismissed item 不应被 processed（除非先恢复为 pending）。后端修改：`AiStudyCardPendingItem` 模型新增 `STATUS_PROCESSED = 'processed'` 常量；`AiStudyCardPendingItemService` 新增 `markPendingItemProcessed()` 私有方法（安全幂等，只更新 status=pending 的 item，created/duplicate 后调用）+ `emptyPendingLifecycleInfo()` 方法（用于 ai_recommended）+ `generateCardsFromConfirmedCandidates()` 返回结果中每个 item 都包含 `pending_item_id` / `pending_item_status_before` / `pending_item_status_after` / `pending_item_processed` / `pending_item_process_reason` 字段；`listPending()` 支持 `status=processed` 过滤；`AiStudyCardPendingItemController` validator 接受 `processed`；`skippedResult` 和 `$failed` 数组也包含 lifecycle 字段。前端修改：`AiStudyCardPendingListDialog.vue` 新增第三个 tab "已处理" + processed items 列表（只读，无操作按钮，显示"状态：已处理 | 处理于 YYYY-MM-DD HH:MM"）；`AiStudyCardDesktopWorkflow.vue` 新增 `aiPendingProcessedItems` data 字段 + `loadAiPendingDismissedItems()` 同时加载 processed items + `confirmGenerateCards()` 成功后刷新 pending list（自动从 pending 视图消失，processed 视图可见）。新增 `tests/Feature/AiStudyCardPendingLifecycleTest.php` 15 项测试（112 assertions）：user_selected+created/duplicate 后 pending→processed / user_selected+failed/skipped 后保持 pending / ai_recommended+created/duplicate 不修改 pending item / dismissed item 不被 processed / cross-user/cross-language 隔离 / 不写 ReviewLog / 不改已有 FSRS / 不创建 legacy word ReviewCard / `?status=processed` 查询 / response 包含 lifecycle 字段 / 重复调用幂等。修改 `tests/Feature/AiStudyCardPendingItemTest.php` `test_generate_cards_deduplicates_duplicate_candidates` 适应新 lifecycle 逻辑（第二次调用变为 skipped 而非 duplicate，幂等）。自动测试 13 个套件全绿（554 tests / 0 failures）：AiStudyCardPendingLifecycleTest 15 passed (112 assertions) / AiStudyCardPendingItemTest 86 passed (486 assertions) / AiStudyCardDesktopWorkflowDeepModuleGuardTest 27 passed (221 assertions) / VocabularySideBoxChineseTextIntegrityTest 5 passed (42 assertions) / AiStudyCardV5DesktopArchitectureGuardTest 17 passed (149 assertions) / AiStudyCardDesktopWorkflowArchitectureGuardTest 22 passed (293 assertions) / VocabularyBoxV5UiGuardTest 16 passed (135 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)；`npm run development` 编译成功（5375ms）。MCP Chrome 真实双 viewport 完整生命周期验收全通过：A. 宽屏 1920x900 SideBox：construed pending → 完整 V3/V4/V5 流程 → POST /generate-cards 200 → 结果展示"已生成 1 张学习卡" + "已从待解释移至已处理" chip → pending list 自动刷新"待解释 (0) / 已处理 (1)" → processed 视图显示 construed "状态：已处理 | 处理于 2026-07-07 00:32" → /reviews/senses 显示 sense card #87 → 数据库 pending item #5 status=processed / ReviewLog 13 未新增 / TARGET_WORD 6 未新增 / TARGET_SENSE 34 含 #87 → Network 全部 127.0.0.1:8000 → Console 仅 WebSocket 降级；B. 半屏 900x900 VocabularyBox：entanglement pending → 完整 V3/V4/V5 流程 → POST /generate-cards 200 → 结果展示"已生成 1 张学习卡" + "已从待解释移至已处理" chip → pending list 自动刷新"待解释 (0) / 已处理 (2)"（construed + entanglement）→ 数据库 pending item #6 status=processed / ReviewLog 13 未新增 / TARGET_WORD 6 未新增 / TARGET_SENSE 35 含 #88 → Network 全部本地 → Console 仅 WebSocket 降级。**不新增产品大功能；只做 pending item 生命周期收口；不进入 V6；不自动调 AI；不写 ReviewLog；不改 FSRS；不创建 legacy word ReviewCard；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不回滚 deep-module split；不实现手机端/BottomSheet；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；1000% 是 10 个子阶段合计提升，不是固定五条主线虚假上涨。** |

## 3. 当前未最终关闭的事项

本节只放真实未完成事项。已完成任务详情进入 `docs/plans/linguacafe-master-plan.md`，历史材料进入 `docs/HISTORY_INDEX.md`。

- **架构收口阶段已结束**（Codex-FinalArchitectureClosureTargetMode-1）：总体架构收口 100% 不代表全项目完成，只代表旧系统地基已检查、sense-only 复习主线边界清楚、AI 示意卡第一版可进入开发设计。详见 `docs/plans/final-architecture-closure-report.md`。
- **AI 示意卡 V3 安全生成包已实现**：详见 `docs/plans/ai-study-card-v3-safe-preview-package-plan.md`。已取消项恢复按钮、真实预览内容、安全生成包已落地。
- **AI 示意卡 V4 AI 推荐词确认闭环已实现**：详见 `docs/plans/ai-recommendation-confirmation-loop-plan.md`。AI 推荐词粘贴导入、去重、默认不选、用户确认、最终候选包已落地。
- **AI 示意卡 V5 用户确认生成学习卡闭环已实现**：详见 `docs/plans/current-working-handoff.md` §2 任务表。V5 已实现从 final_candidates_package 到 confirmed WordSense + target_type=sense ReviewCard + 来源例句（含 synthetic sentence_id）的最小生成闭环。V5 hardening 已收口反向校验 + sense_zh 必填 + sense_en 可空 + AI reason 不自动填 sense_zh + synthetic occurrence + payload 增强。**V5 桌面端架构收敛已完成（GM52-AIStudyCardV5-DesktopArchitectureConvergence-1000-1）**：`VocabularySideBox.vue` 与 `VocabularyBox.vue` 已共用 `AiStudyCardGenerateCardsService.js` + `AiStudyCardGenerateCardsDialog.vue` + `AiStudyCardGenerateCardsResult.vue`，由 `AiStudyCardV5DesktopArchitectureGuardTest`（17 项）+ `VocabularyBoxV5UiGuardTest`（16 项）锁定收敛边界。**桌面端 V1-V5 工作流 feature island 收敛已完成（GM52-AIStudyCardV5-DesktopWorkflowFeatureIsland-1000-2）**：新增 `AiStudyCardDesktopWorkflow.vue`（约 1019 行 feature island 组件，包含 V1-V5 全部模板/data/methods）+ `AiStudyCardRecommendationParserService.js`（3 个纯函数）+ `AiStudyCardPendingWorkflowService.js`（6 个 API 函数 + re-export 3 个）；两个父组件 `VocabularySideBox.vue` 与 `VocabularyBox.vue` 只负责挂载该组件，不再各自维护完整 AIStudyCard 流程；由 `AiStudyCardDesktopWorkflowArchitectureGuardTest`（22 项 / 293 assertions）锁定 feature island 边界。AI 真实推荐（自动调 AI）、AI 自动释义、真实 AI provider、API key、自动刷卡评分仍未实现（V6 范围）。
- **前端复习入口统一第一轮已实现**：详见 `docs/plans/frontend-review-entry-unification-plan.md`。首页"开始复习"和导航"复习"指向 `/reviews/senses`；`/senses/review`、`/review-cards/manage`、legacy `/review/false/-1/-1` 保留。
- **Codex-ProjectDocsGovernanceTargetMode-1**：
  - 本轮只做文档治理，不改业务代码和测试；
  - 新增入口索引、历史索引、ADR-0002、spec→harness 候选清单；
  - 旧 `CURRENT_STATUS` / `NEXT_TASK` / `FSRS_PHASE*` / 旧 handoff 已降权为历史参考；
  - 完成后仍由网页端总设计师选择下一任务，不自动进入下一阶段。
- **AIStudyCardGenerationWorkflow**：
  - V1 pending marker 已实现，V2 列表/取消/预览雏形已实现，V3 已取消视图/恢复按钮/真实预览/安全生成包已实现，V4 AI 推荐词粘贴导入/去重/默认不选/用户确认/最终候选包已实现，V5 用户确认生成学习卡闭环（confirmed WordSense + target_type=sense ReviewCard + 来源例句）已实现并完成 hardening（反向校验 + sense_zh 必填 + sense_en 可空 + AI reason 不自动填 sense_zh + synthetic sentence_id + payload 增强）；
  - V6 仍未实现：AI 真实推荐（自动调 AI）、AI 自动释义、真实 AI provider、API key 安全存储、自动刷卡评分；
  - 后续任何 V6 / 真实 AI 调用 / 自动推荐 / 自动释义前必须先过 Architecture Gate 与 ADR，不删除现有 SenseMappingReview / SenseReview 能力，不删除 legacy word card 兼容层。
- **旧版入口与 Finished reading 护栏**：
  - 普通查词 UI 的旧版释义入口已完成最小隐藏，兼容字段和后端 legacy 兼容层未删除；
  - Finished reading 自动 known 分支新增测试护栏，并修复当前语言过滤缺口；
  - 本地 MySQL 未启动时 PHP Feature tests 会被数据库连接阻塞，不能用 SQLite 可靠替代（旧迁移含 MySQL collation）。

## 4. 当前产品决策

| 决策 | 内容 |
|------|------|
| 归档语义 | 归档 = 暂停复习卡，不是删除 |
| archiveSense occurrence 引用 | 不清空 review_card_id 和 auto_fsrs_allowed 是当前接受的行为（与 removeSenseFromReviewSystem(false) 不一致但已做产品取舍） |
| 永久删除 ReviewLog | 默认保留复习历史，当前不提供"同时删除复习历史"选项 |
| EncounteredWord restore | 使用 encountered_word_id 匹配是安全设计，不是 bug |
| 归档 restore | 归档不恢复 EncounteredWord 是正确语义（归档 ≠ 删除） |
| ReviewLog 保留 | 不是 bug，是有意设计 |
| 删除提示文案 | 必须提示"复习历史已保留" |
| rejectSense() | 遗留方法，低优先级，无调用方 |
| 复习主入口统一 | 前端不展示"词义确认/词义复习"内部概念，主复习入口统一叫"复习" |
| AI 译文 ≠ AI 示意卡 | AI 译文只服务阅读理解，不自动生成复习卡 |
| 用户选词优先 | 阅读页点击单词或拖动选择词组，手动添加释义后直接生成可复习示意卡 |
| 待 AI 解释词 | 用户可以把不会解释的词标记为"待 AI 解释" |
| AI 推荐词不重复 | AI 推荐词必须排除用户已选择的词 |
| AI 推荐词默认不选 | "AI 生成示意卡"弹窗中 AI 推荐词默认全不选，提供"全选"按钮 |
| 确认后才生成 | 只有被用户确认的 AI 推荐词才进入示意卡生成 |
| legacy word card | 只作兼容，不作为新功能和日常复习主线 |
| Finished reading | 保留章节完成能力；自动 known 只处理当前用户/当前语言黄色新词，不写 WordSense/ReviewCard/ReviewLog/FSRS，不等同复习完成 |
| 阅读中刷卡 / 多例句轮换 | 路线已冻结为 WordSense-only 与真实来源例句池；当前未实现 |

## 5. 下一候选方向

> 架构收口已结束。下一阶段应进入明确的最小实现任务，不再继续无限侦查。每个候选前必须先过 Architecture Gate 与 ADR（如涉及接口、DB、FSRS、删除/归档/恢复）。

### A. AI 示意卡后续：V5 hardening / source binding / desktop architecture convergence / V6 前置设计

- V1-V5 已实现：阅读页点词 → 侧栏"待 AI 解释"按钮 → pending item → 列表/取消/恢复 → preview-package → 粘贴 AI 推荐词 → final-candidates-package → 用户确认中文释义 → 生成 confirmed WordSense + target_type=sense ReviewCard + 来源例句（含 synthetic sentence_id）→ 新卡进入 `/reviews/senses`。
- V5 已实现最小生成闭环（commit `bff78f1`）。V5 hardening（commit `3d14478`）已收口：ai_recommended 反向校验 final_candidates_package、user_selected item_id + word/lemma 反向校验、sense_zh 必填 sense_en 可空、AI reason 不自动填 sense_zh、synthetic sentence_id 来源绑定、结果 payload 含 occurrence_created / source_binding_status。
- **V5 桌面端架构收敛已完成**（GM52-AIStudyCardV5-DesktopArchitectureConvergence-1000-1，commit `e53d116`）：宽屏 `VocabularySideBox.vue` 与半屏 `VocabularyBox.vue` 共用 `AiStudyCardGenerateCardsService.js` + `AiStudyCardGenerateCardsDialog.vue` + `AiStudyCardGenerateCardsResult.vue`；`AiStudyCardV5DesktopArchitectureGuardTest` 17 项 + `VocabularyBoxV5UiGuardTest` 16 项锁定收敛边界；MCP Chrome 双 viewport 回归全通过。明确不处理 `VocabularyBottomSheet.vue`（当前项目无手机端）。
- **桌面端 V1-V5 工作流 feature island 收敛已完成**（GM52-AIStudyCardV5-DesktopWorkflowFeatureIsland-1000-2，commit 见 §2 任务表）：把 V1（pending item 标记）→ V2（待解释列表/取消/恢复）→ V3（preview-package）→ V4（粘贴 AI 推荐词 / parse / dedupe / final-candidates-package）→ V5（generate-cards）的完整桌面端流程收敛到一个独立 feature island 组件 `AiStudyCardDesktopWorkflow.vue`（约 1019 行），新增 `AiStudyCardRecommendationParserService.js`（3 个纯函数）+ `AiStudyCardPendingWorkflowService.js`（6 个 API 函数 + re-export 3 个）；两个父组件只负责挂载该组件，不再各自维护完整 AIStudyCard 流程；由 `AiStudyCardDesktopWorkflowArchitectureGuardTest` 22 项 / 293 assertions 锁定边界；MCP Chrome 双 viewport 完整 V1-V5 回归全通过。明确不处理 `VocabularyBottomSheet.vue`。
- 下一候选方向：V6 前置设计（AI 自动推荐 / AI 自动释义 / 真实 AI provider / API key 安全存储）。V6 仍未实现：自动调用 AI、API key、AI 自动推荐、AI 自动释义、真实 AI provider、自动刷卡评分。任何 V6 实现前必须先过 Architecture Gate 与 ADR。
- 继续禁止：自动调 AI、自动生成 WordSense/ReviewCard/ReviewLog（V5 之外的新入口）、改 FSRS、改删除/归档/恢复、删除 legacy word card 兼容层、把 V5 复制进 `VocabularyBottomSheet.vue`。

### B. 前端复习入口统一后续收口

- 第一轮已完成：首页"开始复习"与导航"复习"进入 `/reviews/senses`，旧路由全保留。
- 后续只可小步收口：评估 `/senses/review` 是否 alias 到 `/reviews/senses`，以及复习卡管理是否进一步移入高级/设置区域。
- 任何删除页面或下线 legacy word review 的动作必须走 ADR。

### C. TextBlockService-RemainingExtractionScouting-1

- phrase/index 最小 characterization tests 已在 Codex-ArchitectureOptimizationLoop-1 中补充；
- 后续若继续该方向，建议聚焦 tokenizer/fallback 或旧 ReaderDataService fallback 分支的只读侦查；
- 不直接拆，先看剩余职责和测试缺口；
- 适合 Codex 接盘，但必须先给禁止范围和验收命令。

### D. FsrsRescheduleConfirmApply-SafeWriteContractTests-1

- 拒绝写入路径的最小 contract tests 已在 Codex-ArchitectureFinalGoalMode-1 中补充；
- 已锁定 `apply=true` 高风险未二次确认、blocked 超量两类场景均不写 ReviewCard、不建 snapshot、不写 ReviewLog；
- 后续若继续该方向，建议只读侦查 stale candidate / params 变化 / snapshot appliedCount=0 等剩余边界，不直接改 FSRS 语义。

### E. SpecToHarness-Hardening-1

- 参考 `docs/plans/spec-to-harness-candidates.md`；
- 只选择一个软约束转测试 / smoke / harness；
- 不把候选清单当作实现授权。

### F. Codex 大任务候选

由网页端总设计师决定。Codex 任务不应从脏上下文开始，必须先看：
1. `docs/plans/current-working-handoff.md`（本文）
2. `docs/plans/linguacafe-master-plan.md`
3. `docs/plans/vibe-coding-collaboration-rules.md`
4. `docs/plans/repo-architecture-hotspot-audit.md`
5. `docs/plans/final-architecture-closure-report.md`（架构收口结论）
6. 按需读 `docs/DOCUMENTATION_INDEX.md`、`docs/HISTORY_INDEX.md`、`docs/adr/ADR-0002-sense-only-and-ai-study-card-boundaries.md`

## 6. Codex 交接原则

1. Codex 可以接复杂任务，但不能取消边界。
2. Codex 任务可以少一点微观步骤，但必须有目标、禁止范围、验收命令和报告格式。
3. Codex 不应自动修改所有文档。
4. Codex 不应自动进入下一任务。
5. Codex 执行后必须由网页端总设计师核验 GitHub 最新代码。
6. CodeBuddy 只做风险线索复核，不做最终判断。
7. OpenCode / Codex 报告都不能直接作为事实。
8. MCP Chrome 页面任务不能用 API 代替。
9. 如果 Codex 改代码，必须说明：改了哪些文件、跑了哪些测试、哪些验收无法完成。
10. Codex 应先读 current-working-handoff，再读 master plan，再读相关模块文档，不从头扫描所有旧文档。

## 7. 当前主线进度

以下百分比是总设计师用于沟通的产品进度估算，不是精确测试覆盖率。后续会根据 GitHub 最新代码、测试和真实页面验收调整。不得把估算进度当成最终完成承诺。

> 文档与协作规则治理属于阶段性支撑任务，不作为固定五条主线之一；如某轮任务专门处理文档规则，可在本轮说明中临时提及。

| 主线 | 进度 | 说明 |
|------|------|------|
| 总体架构收口 | ≈ 100% | 架构收口阶段已结束（Codex-FinalArchitectureClosureTargetMode-1）。100% 不代表全项目完成，只代表旧系统地基已检查、sense-only 复习主线边界清楚、AI 示意卡第一版可进入开发设计、高风险区清楚、文档入口清楚、下一轮不应继续无限侦查。详见 `docs/plans/final-architecture-closure-report.md`。 |
| 复习主线稳定 | ≈ 96% | WordSense/ReviewCard/FSRS 核心链路已锁定；V3 新增 dismissed 列表/restore/preview-package 反向 contract tests，进一步确认不写 WordSense/ReviewCard/ReviewLog/EncounteredWord；入口统一后日常复习主线指向 `/reviews/senses`。 |
| 页面真实验收 | ≈ 100% | V3 MCP Chrome 真实页面验收 28 项全通过：阅读页点词 → 标记 → 列表 → 取消 → 已取消视图 → 恢复 → 真预览弹窗 → 勾选/取消勾选 → 全不选禁用 → 准备生成 → 安全包 JSON → 复制 → 无 AI 调用 → 无学习数据写入 → 主入口/旧入口正常 → console/network 检查。 |
| AI 示意卡规划 | ≈ 100% | V1 pending marker + V2 列表/取消/预览雏形 + V3 已取消视图/恢复/真实预览/安全生成包已落地，路线完全清晰。未完成：AI 推荐词、AI 释义生成、WordSense/ReviewCard 生成闭环、真实 AI 调用。 |
| 前端入口整理 | ≈ 100% | 第一轮入口统一已完成：主入口文案为「复习」并指向 `/reviews/senses`，首页「开始复习」进入 sense-only 主线，旧路由保留。V3 列表/已取消视图/预览/安全包入口与复习入口完全协调。后续仅剩更细的 alias/高级分组收口（不影响日常使用）。 |

### 7.1 子阶段进度

| 子阶段 | 进度 | 说明 |
|--------|------|------|
| AI 示意卡生成闭环 | ≈ 95% | V3 已取消视图/恢复按钮/真实预览/安全生成包已完成（95%）。**这个 95% 是「AI 示意卡生成闭环」子阶段的进度，不是固定五条主线的虚假上调。** AI 真实推荐、AI 释义生成、WordSense/ReviewCard 生成闭环、真实 AI 调用仍未实现。 |
| AI 生成安全契约 | ≈ 85% | V3 安全生成包（schema_version=ai-study-card-preview-package-v1）+ V4 最终候选包（schema_version=ai-study-card-final-candidates-v1）已完成。V4 safety_flags 6 条：no_ai_called_by_linguacafe / ai_response_pasted_by_user / no_review_card_created / no_word_sense_created / no_fsrs_changed / user_confirmation_required_before_card_generation；generation_rules 5 条。V3 + V4 共 32 个反向 contract tests 覆盖用户/语言/状态隔离 + 去重 + 默认不选 + 空结果 + 数量上限。**85% 是子阶段进度，不是固定五条主线的虚假上调。** API key 安全存储、真实 AI 调用边界、用户确认后生成 WordSense/ReviewCard 仍未实现。 |
| AI 推荐词确认闭环 | ≈ 80% | V4 新增子阶段。粘贴导入、去重、默认不选、用户确认、最终候选包已落地。**80% 是子阶段进度，不是固定五条主线的虚假上调。** AI 真实推荐（自动调用 AI 获取推荐词）仍未实现。 |
| 旧版入口清理执行 | ≈ 80% | 普通查词界面的旧入口文案已隐藏，并加源码 guard；后端兼容层和 legacy route/service/tests 保留。**80% 是子阶段进度，不是固定五条主线的虚假上调。** 更深层删除必须另做依赖审计。 |
| Finished reading 安全护栏 | ≈ 70% | Feature test 覆盖 yellow→known、green/WordSense/ReviewCard/ReviewLog/FSRS/用户/语言隔离，并修复语言过滤缺口。本轮新增「完成阅读」确认弹窗（说明影响 + 取消/确认），不污染真实数据。**70% 是子阶段进度，不是固定五条主线的虚假上调。** |
| 阅读中复习 / 多例句轮换路线 | ≈ 65% | 路线冻结文档明确 WordSense-only、真实来源例句、熟词僻义分区、surface/lemma 绑定原则。已实现多例句池、题面例句轮换、补充例句不重复、多来源溯源、词形原型绑定前置、known-sense 候选、例句池/来源查询性能优化。本轮新增 inline confirmation management surface（read-only list + 单条 revoke + 管理页 + preview panel 入口链接），阅读中刷卡前置匹配 70%→75%。这些是子阶段进度，不是固定五条主线虚假上涨。**阅读中刷卡评分仍未实现。AI 不生成例句。熟词僻义仅前置结构，AI 判断仍未实现。inline confirmation 不等于评分，不写 ReviewLog，不改 FSRS。撤销不等于忘记，不删除 WordSense/ReviewCard/ReviewLog。** |

> 如果任务失败或 Incomplete，对应进度不得上调。
> 如果一个任务完成后不会推动任何固定主线进度，就不得作为 OpenCode / Codex / Trae 的单独任务派发；应合并到能推动主线进度的复合任务中。纯小修正只能作为复合任务的附带项。
> "总体架构收口 100%"是进入新功能开发前的架构收口 100%，不是全项目完成。不要把它写成全项目完成。

## 8. 临时文档使用规则

1. 本文是短期工作台，不是永久事实源。
2. 每次完成一个阶段，可以更新本文。
3. 过期事项要移动到 master plan 或删除。
4. 不允许让本文和 master plan 冲突。
5. 如冲突，以 master plan 为准，并修正本文。

## Recent Update: Codex-FinalArchitectureClosureTargetMode-1

- Added three frozen-plan documents:
  - `docs/plans/final-architecture-closure-report.md` — final architecture closure report (Overall architecture closure 81% → 100%).
  - `docs/plans/ai-study-card-v1-frozen-plan.md` — AI study card v1 frozen plan (AI study card planning 25% → 55%).
  - `docs/plans/frontend-review-entry-unification-plan.md` — frontend review entry unification frozen plan (Frontend entry cleanup 50% → 65%).
- MCP Chrome read-only page observation covered: reading page (`/chapters/read/5`), vocabulary side box, AI reading assist button, review entry (homepage "开始复习" + nav "单词复习/词义确认"), sense confirmation entry (`/senses/review`), review card management entry (`/review-cards/manage`).
- Key findings from page observation:
  - "待 AI 解释" button does not exist yet — confirms AI study card v1 entry position.
  - Navigation still exposes internal names "单词复习 / 词义确认".
  - Homepage "开始复习" button points to `/review/false/-1/-1` (legacy word review), not `/reviews/senses`.
  - legacy word card compatibility layer exists as "旧版释义（兼容）" label.
  - Review card management page shows 7 sense cards, no legacy word cards visible.
- Updated progress to: Overall architecture closure 100%, Review mainline 91%, Page real acceptance 91%, AI study card planning 55%, Frontend entry cleanup 65%.
- "Overall architecture closure 100%" means the architecture-closure phase is over and AI study card v1 can enter development design; it does NOT mean the whole project is finished.
- This task did NOT change business code, tests, Vue, Controller, Service, routes, migration, DB schema, FSRS semantics, delete/archive/restore semantics, ReviewLog retention, legacy word card compatibility layer, SenseReview, or SenseMappingReview.

## Recent Update: Codex-AIStudyCardV1-And-ReviewEntryUnification-1

- Implemented AI study card v1 pending marker: new `ai_study_card_pending_items` table, `AiStudyCardPendingItem` model, `AiStudyCardPendingItemService`, `AiStudyCardPendingItemController`, and `POST /ai-study-card/pending-items`.
- Added reading lookup button「待 AI 解释」in `VocabularySideBox.vue` and responsive `VocabularyBox.vue`; it records a pending item only and returns success/existing feedback.
- Added `tests/Feature/AiStudyCardPendingItemTest.php` for auth, user/language isolation, duplicate idempotency, and reverse contracts confirming no WordSense/ReviewCard/ReviewLog/EncounteredWord writes.
- Unified frontend review entry round 1: homepage「开始复习」and nav「复习」point to `/reviews/senses`; `/senses/review`, `/review-cards/manage`, and `/review/false/-1/-1` remain accessible.
- Progress updated to: Overall architecture closure 100%, Review mainline stability 94%, Page real acceptance 96%, AI study card planning 90%, Frontend entry cleanup 92%.
- Still not implemented: AI recommendation modal, AI meaning generation, automatic WordSense/ReviewCard creation, FSRS integration, full AI study card generation loop, or final legacy entry cleanup.

## Recent Update: GLM-AIStudyCardV2-GenerationLoop-1

- AI study card v2 generation loop phase 1 is implemented: pending item list, dismiss/restore, generation preview modal placeholder.
- New backend endpoints: `GET /ai-study-card/pending-items` (with chapter_id filter), `POST /ai-study-card/pending-items/{id}/dismiss`, `POST /ai-study-card/pending-items/{id}/restore`.
- `createOrGetPending()` refactored: if same key exists as dismissed, restore to pending instead of creating duplicate.
- Frontend: `VocabularySideBox.vue` and `VocabularyBox.vue` now include "待 AI 解释列表" button, list panel with cancel, "生成 AI 示意卡" button, and preview modal placeholder with safety notice and rule preview.
- Added 16 new V2 feature tests (23 tests / 105 assertions total, all green). Covers list auth, user/language isolation, chapter filter, dismiss/restore, idempotency, reverse contracts (no WordSense/ReviewCard/ReviewLog writes).
- MCP Chrome real-page acceptance 24/24 passed: login → reading page → click word → mark → list → cancel → re-mark → preview modal → no AI calls → no learning data writes → main/old entry points work → console/network clean.
- Five-line progress: Overall architecture closure 100%, Review mainline stability 96%, Page real acceptance 100%, AI study card planning 100%, Frontend entry cleanup 98%.
- New sub-phase: AI study card generation loop 70%. **This 70% is the sub-phase progress, NOT a fake uplift of the five main lines.** AI recommended words, AI meaning generation, WordSense/ReviewCard generation loop, and real AI calls are still not implemented.
- No AI calls, no API key saved, no WordSense/ReviewCard/ReviewLog created, no FSRS changes, no delete/archive/restore changes, no SenseReview/SenseMappingReview/legacy word card removal.

## Recent Update: GLM-AIStudyCardV3-SafePreviewPackage-1

- AI study card v3 safe preview package is implemented: dismissed-item restore button, real preview content, safe preview package.
- Backend: extended `GET /ai-study-card/pending-items` to accept `status=pending|dismissed|all`; added `POST /ai-study-card/pending-items/preview-package` that returns a safe JSON package (schema_version=ai-study-card-preview-package-v1) with selected_items, generation_rules, and safety_flags (no_ai_called / no_review_card_created / no_word_sense_created / no_fsrs_changed).
- Frontend: `VocabularySideBox.vue` and `VocabularyBox.vue` upgraded with pending/dismissed view toggle, restore button for dismissed items, real preview modal (user-selected words list, source sentence, chapter position, count, status, safety notice, checkbox per item with "select all none → disable 准备生成", AI-recommended words placeholder area, future generation rules), "准备生成" button triggering backend preview-package endpoint, JSON display, and "复制生成包" button with success/failure toast.
- Added 14 new V3 feature tests (37 tests / 184 assertions total, all green). Covers dismissed list auth/isolation, restore idempotency + no learning data, preview-package auth/user/language/status isolation, empty item_ids, reverse contracts (no WordSense/ReviewCard/ReviewLog/FSRS changes, no pending status change).
- MCP Chrome real-page acceptance 28/28 passed: login → reading page → click word → mark → list → cancel → dismissed view → restore → real preview modal → checkbox toggle → all-uncheck disables button → 准备生成 → safe package JSON → copy → no AI calls → no WordSense/ReviewCard/ReviewLog writes → main/old review entry points work → console/network clean.
- Five-line progress: Overall architecture closure 100%, Review mainline stability 96%, Page real acceptance 100%, AI study card planning 100%, Frontend entry cleanup 100%.
- Sub-phase progress: AI study card generation loop 70% → 95%; AI generation safety contract 0% → 55%. **These are sub-phase progress, NOT a fake uplift of the five main lines.** AI recommended words, AI meaning generation, WordSense/ReviewCard generation loop, and real AI calls are still not implemented.
- No AI calls, no API key saved, no WordSense/ReviewCard/ReviewLog created, no FSRS changes, no delete/archive/restore changes, no SenseReview/SenseMappingReview/legacy word card removal.
- Did NOT enter the next task automatically.

## Recent Update: GLM-AIRecommendationConfirmationLoop-V4-1

- AI study card v4 AI recommendation confirmation loop is implemented: paste AI recommendation JSON, dedupe, default unchecked, user confirmation, final candidates package.
- Backend: added `POST /ai-study-card/pending-items/final-candidates-package` that returns a safe JSON package (schema_version=ai-study-card-final-candidates-v1) with user_selected_items / ai_recommended_selected_items / ai_recommended_unselected_items / dedupe_summary / generation_rules (5 rules) / safety_flags (6 flags: no_ai_called_by_linguacafe / ai_response_pasted_by_user / no_review_card_created / no_word_sense_created / no_fsrs_changed / user_confirmation_required_before_card_generation). Backend enforces user/language/status triple isolation, secondary dedupe (AI vs user-selected, AI internal, unselected vs selected), empty-result 422, post-query empty 422, and size limits (max 100 user-selected, max 200 AI recommendations).
- Frontend: `VocabularySideBox.vue` and `VocabularyBox.vue` upgraded with V4 paste-AI-recommendation-JSON textarea, parse/clear buttons, parse error message, parse summary (original/valid/dropped-missing-word/dropped-duplicate-with-user/dropped-ai-internal-duplicate), AI recommendation list with checkbox per item default unchecked, select-all/select-none for AI recommendations, visual separation between user-selected words and AI recommendations (v-divider), "生成最终候选包" button in v-card-actions, final candidates package JSON display, and "复制最终候选包" button with success/failure toast.
- Added 18 new V4 feature tests (56 tests / 294 assertions total, all green). Covers auth, user/language/status isolation, AI dedupe vs user-selected, AI internal dedupe, default-unchecked reflected in data structure, empty selected + empty AI returns 422, only user-selected without AI allowed, invalid AI does not crash, no WordSense/ReviewCard/ReviewLog creation, no pending status change, no FSRS field changes (fsrs_state/fsrs_due_at/fsrs_stability/fsrs_difficulty/fsrs_reps/fsrs_lapses/fsrs_last_reviewed_at/fsrs_enabled), safety_flags correct, unselected AI deduped against selected AI, max items limit, source_preview_package preserved.
- MCP Chrome real-page acceptance 33/33 passed: login → reading page → click word → mark → list → preview modal → safe package → paste valid JSON (agency + mediation) → parse summary 2/2/0 → default 0 checked → paste duplicate JSON (substantive + agency + Agency) → parse summary 3/1/1/1 → paste malformed JSON → "JSON 格式错误" error → no crash → select one AI recommendation → select all → select none → re-select agency → 生成最终候选包 → final package JSON with schema_version=ai-study-card-final-candidates-v1, user_selected_items(3), ai_recommended_selected_items(1: agency), ai_recommended_unselected_items(1: mediation), dedupe_summary, generation_rules(5), safety_flags(6) → 复制最终候选包 → "已复制到剪贴板" → no external AI network requests (only local POST /final-candidates-package 200 OK) → no WordSense/ReviewCard/ReviewLog writes → /reviews/senses main entry works → /review-cards/manage old entry works → console/network clean.
- Five-line progress: Overall architecture closure 100%, Review mainline stability 96%, Page real acceptance 100%, AI study card planning 100%, Frontend entry cleanup 100%.
- Sub-phase progress: AI study card generation loop 95% (unchanged); AI generation safety contract 55% → 85%; AI recommendation confirmation loop 0% → 80% (new sub-phase). **These are sub-phase progress, NOT a fake uplift of the five main lines.** AI real recommendation (auto AI call), AI meaning generation, WordSense/ReviewCard generation loop, and real AI calls are still not implemented.
- Regression: ReviewFsrsTest 61/364, FsrsSchedulingServiceTest 9/46, WordSense (DestroyRestore+Test) 149/595 all green. npm run development compiled successfully.
- No AI calls, no API key saved, no WordSense/ReviewCard/ReviewLog created, no FSRS changes, no delete/archive/restore changes, no SenseReview/SenseMappingReview/legacy word card removal.
- Did NOT enter the next task automatically.

## Recent Update: Trae-ExamplePool-ReviewRotation-SourceCarousel-1

- Multi-example pool + review-page question example rotation + answer-side supplementary example non-duplication + multi-source carousel + Finished reading confirmation dialog are implemented.
- New backend service `WordSenseExamplePoolService`: collects real-source example candidates from `WordSenseOccurrence` (status=BOUND, sentence_en non-empty) + card example fallback. Dedupe by chapter + sentence text; sentence-only dedupe for card fallback against any occurrence. Read-only: no ReviewLog, no WordSense, no ReviewCard, no FSRS writes. No AI calls.
- `SenseReviewCardSerializerService` payload extended with `example_candidates`, `example_candidates_count`, `supplementary_example`. Question example index chosen by stable seed (review_card_id * 31 + fsrs_reps * 7 + day-of-year, crc32 hash). Supplementary index chosen by independent seed (review_card_id * 17 + fsrs_reps * 13 + day-of-year + 1009), guaranteed different from question index. Supplementary is null when only one candidate exists.
- `SenseSourceContextService::sourceContextList()` returns `{ sense_id, sources: [...], count }` with up to 3 distinct chapter sources. Same-chapter duplicates collapse. Falls back to single-element source list when no chapter sources exist. New route `GET /senses/{id}/source-context-list`.
- `SenseExampleDialog.vue` rewritten to support multi-source carousel: 来源 1/N chip, prev/next buttons, scrollTargetIntoView on source change. Single-source cards do not show switching buttons (rule compliance). `context` computed preserved as legacy alias.
- `SenseReview.vue` answer side adds supplementary-example block with defensive dedup check (supplementary_example.sentence_en === currentCard.example_sentence_en → null). `viewSource()` now calls `/source-context-list` endpoint.
- `TextReader.vue` 「完成阅读」 button now opens confirmation dialog first. Dialog explains: yellow (new) words marked known; green (learning) words do not enter review; existing WordSense review cards and review history are not affected. Cancel does not execute; confirm runs the original `finish()` method. Backend semantics unchanged.
- Added 18 new feature tests (WordSenseExamplePoolTest 12 + SenseSourceContextMultiSourceTest 6, all green): multiple occurrences produce multiple candidates; duplicate sentences collapsed; card example fallback; card fallback dedupe against occurrences; question rotation not always first; question rotation stable for same seed; question rotation changes with reps; supplementary different from question; supplementary null for single candidate; pool does not write ReviewLog/WordSense/ReviewCard; serializer payload includes candidates + supplementary; serializer payload supplementary null for single candidate; source-context-list shape; multiple distinct chapters produce multiple sources; same-chapter duplicates collapse; no-chapter fallback to single source list; read-only; cross-user isolation.
- Regression: FinishedReadingSafetyTest 2/27, LegacyEntryUiGuardTest 1/12, ReviewFsrsTest 61/364, FsrsSchedulingServiceTest 9/46, WordSense (DestroyRestore+ExamplePool+Test) 161/680, SenseSourceContextTest 29/186 all green. npm run development compiled successfully.
- MCP Chrome real-page acceptance: login (1816529781@qq.com) → /reviews/senses question example displayed → 显示答案 → single-example card shows no duplicate supplementary (rule compliance) → 更多 → 查看原文 → single-source dialog shows no switching buttons (rule compliance) → /chapters/read/5 → 「完成阅读」 button → confirmation dialog with correct text ("确认完成阅读？" + yellow/green/history explanation + 取消/确认完成) → click 取消 → dialog closes, page does NOT enter "阅读完成" state → console only WebSocket (Pusher local log fallback, expected per AGENTS.md) → network all 200.
- Five-line progress: Overall architecture closure 100%, Review mainline stability 96%, Page real acceptance 100%, AI study card planning 100%, Frontend entry cleanup 100%.
- New sub-phase progress: multi-example pool 0% → 60%; review-page question rotation 0% → 50%; multi-source carousel 0% → 30%; Finished reading misclick protection 0% → 20%. Total sub-phase uplift: 160%. **This 160% is four sub-phase progress values, NOT a fake uplift of the five main lines.**
- **Reading inline review (阅读中刷卡) is still NOT implemented. AI does NOT generate examples. No AI calls. No WordSense/ReviewCard/ReviewLog created. No FSRS changes. No delete/archive/restore changes. No SenseReview/SenseMappingReview/legacy word card removal. No new migration.**
- Did NOT enter the next task automatically.

## Recent Update: Trae-LemmaKnownSenseBridge-1

- Lemma/surface binding + known-sense candidates + known-sense-new-meaning front-only structure + example-pool N+1 optimization are implemented.
- New backend service `WordSenseKnownSenseService` (read-only): `listConfirmedSensesForLemma(userId, language, lemma)` returns only confirmed WordSense of the current user/language/lemma (excludes rejected/AI-suggested/pending); batch-loads `WordSenseOccurrence` counts via `whereIn` + `groupBy` to avoid N+1; `knownSenseLookupPayload()` returns `{ lemma, has_confirmed_senses, confirmed_senses, known_sense_new_meaning_hint, read_only }`. No ReviewLog/WordSense/ReviewCard/FSRS writes. No AI calls.
- New endpoint `GET /senses/known-sense-lookup?lemma=...&language=...` on `SenseOccurrenceController::knownSenseLookup`. Enforces user/language isolation via `Auth::user()->selected_language` and 403 on language mismatch. 422 on empty lemma.
- `WordSensesList.vue` adds 「已学词义候选」 panel (v-if="knownSenses.length > 0"): success-color header with mdi-bookmark-check icon, count chip, lemma label, per-sense row with 已学 chip + pos chip + FSRS chip + 已复习 N 次 + sense_zh + sense_en. Adds 「熟词僻义」 info alert with explicit "未调用 AI 判断" notice. `fetchKnownSenseLookup` uses stale-guard pattern (latestKnownSenseLookupKey) to avoid race conditions. `effectiveLemma` (studyBase → baseWord → lemma → surface → word) and `language` watcher both trigger `fetchKnownSenseLookup` and `fetchSenses`.
- Lemma/surface binding: reading-page click on a word shows surface (e.g. `geese`) + lemma (e.g. `goose`) + [修改] entry; search/add-sense flow prefers lemma (search box `value=lemma`); user can correct lemma via `VocabularySideBox::saveLemma` → `commit setStudyBase` → `POST /vocabulary/word/update`; later add-sense uses corrected lemma; no unconditional binding of `published`→`publish`. Test `test_add_new_sense_uses_corrected_lemma_after_user_edit` confirms lemma and surface_form stored independently.
- Performance: `WordSenseExamplePoolService::exampleCandidates()` eliminates N+1 Chapter query by batch `whereIn` preloading chapter names. `SenseSourceContextService::sourceContextList()` eliminates per-occurrence `findChapterById` by batch `whereIn` preloading + `limit(12)` + PHP `unique('chapter_id')->take(3)` (cross-database compatible: SQLite GROUP BY + orderByRaw combination fails, so PHP-layer dedupe retained). Returns identical fields and fallback behavior.
- Added 13 new feature tests in `tests/Feature/WordSenseKnownSenseBridgeTest.php`: returns confirmed senses for lemma; isolates by user; isolates by language; excludes rejected; excludes ai_suggested; does not write ReviewLog/WordSense/ReviewCard; returns empty for unknown lemma; normalizes lemma lowercase; returns FSRS fields (has_review_card/fsrs_state/fsrs_reps/fsrs_due_at/fsrs_enabled); returns occurrence_count; knownSenseLookupPayload returns correct shape + read_only flag; add new sense uses corrected lemma after user edit; payload separates known-sense candidates from add-new-sense area.
- Regression: WordSenseExamplePoolTest 12/85, SenseSourceContextMultiSourceTest 6/33, FinishedReadingSafetyTest 2/27, LegacyEntryUiGuardTest 1/12, ReviewFsrsTest 61/364, FsrsSchedulingServiceTest 9/46, WordSense (DestroyRestore+ExamplePool+Test+KnownSenseBridge) 174/718 all green. npm run development compiled successfully (4951ms).
- MCP Chrome real-page acceptance: login (1816529781@qq.com) with isolatedContext → /chapters/read/7 → click `geese` → 单词基础信息 shows `geese → goose [修改]` → 添加新释义 search box value=`goose` (lemma preferred) → 添加为新释义 → 保存新释义 → 取消选择 → re-click `geese` → 「已学词义候选」 panel shows 1 confirmed sense (鹅, 雌鹅, 鹅肉, 弯把熨斗, noun, FSRS chip, 已复习 0 次) + 「熟词僻义」 info alert shows "这个词你学过一些意思，但这里可能是新意思。" + "未调用 AI 判断" → /reviews/senses 显示答案 + 例句正常 → 更多 → 查看原文 多来源溯源 dialog 正常 → console only WebSocket (Pusher local log fallback, expected per AGENTS.md) → network all 200 (including GET /senses/known-sense-lookup?lemma=goose&language=english 200).
- Five-line progress: Overall architecture closure 100% (unchanged), Review mainline stability 96% (unchanged), Page real acceptance 100% (unchanged), AI study card planning 100% (unchanged), Frontend entry cleanup 100% (unchanged). **The five main lines are NOT inflated.**
- New sub-phase progress: lemma-surface binding 10% → 60%; known-sense-new-meaning recognition 15% → 65%; reading-inline-review front-end matching 0% → 40%; multi-example-pool performance & source-query optimization 0% → 20%. Total sub-phase uplift: 160%. **This 160% is four sub-phase progress values, NOT a fake uplift of the five main lines.**
- **Reading inline review scoring (阅读中刷卡评分) is still NOT implemented. No ReviewLog written. No FSRS changes. No real AI calls. AI does NOT generate examples. Known-sense-new-meaning is only a front-end structure; AI judgment for "is this a known-sense-new-meaning case?" is still NOT implemented. No WordSense/ReviewCard created by the new endpoint. No delete/archive/restore changes. No SenseReview/SenseMappingReview/legacy word card removal. No new migration.**
- Did NOT enter the next task automatically.

## Recent Update: GLM-ArchitectureFirst1000-SafeStability-1

- **Round type**: GLM 1000% round 1 (architecture-first, safe-stability first). **不是新功能冲刺。不是阅读中刷卡评分实现。不是 per-occurrence lemma 落库。不是 FSRS / ReviewLog / AI 写入改造。**
- **第一硬原则**：代码安全性和稳定性优先于功能速度。已写入 `vibe-coding-collaboration-rules.md` §27.0 与 `repo-architecture-hotspot-audit.md` §8.5.0。
- **本轮合计 1000% 是 8 个子阶段进度合计，不是固定五条主线虚假上涨。**
- **子阶段 1 安全稳定优先规则写入（+100%）**：`vibe-coding-collaboration-rules.md` §27.0 / §27.4 / §27.5 写入；`repo-architecture-hotspot-audit.md` §8.5.0 写入；`DOCUMENTATION_INDEX.md` §2.5 同步。
- **子阶段 2 MCP sample tracker 创建（+100%）**：新增 `docs/plans/morphology-test-sample-tracker.md`，含 R0/R1/R2 索引、8 类覆盖矩阵、候选词池、填写规则；`spec-to-harness-candidates.md` 与 `mcp-chrome-local-smoke-playbook.md` §8 同步。
- **子阶段 3 MCP 新文章 + 新词真实点击验收（+150%）**：marker `GLM Architecture 1000 Morphology Sample 20260703`，新章节 `/chapters/read/14`，20 个真实 Playwright 点击，8/8 类别覆盖，与 R1 重复率 0/20 = 0%（远低于 30% 阈值），12/20 lemma 验证成功，8/20 P2 残留（已报告未强制），无 ReviewLog/ReviewCard/FSRS 写入。点击全部使用 MCP Chrome 真实浏览器，无 API/axios/fetch 替代。
- **子阶段 4 known-sense lookup 回归护栏（+100%）**：`WordSenseKnownSenseBridgeTest` 新增 8 个测试覆盖空 lemma / 大小写 / 多语言隔离 / 用户隔离 / 只返回 confirmed / pending+ignored+rejected 不返回 / occurrence count 正确 / `read_only=true` / 不写 ReviewLog+WordSense+ReviewCard+FSRS。无 API payload 变更，无 AI 调用，无 migration。
- **子阶段 5 source context / example pool 边缘 case 回归护栏（+150%）**：`WordSenseExamplePoolTest` 新增 10 个测试覆盖空章节 / 找不到 source context / 多语言混合 / 同 sentence 多 occurrence / 多章节来源 / duplicate example 去重 / fallback card_example 不误判为原文 / source context carousel 不重复 / N+1 回归 / 只读不写。`SenseSourceContextMultiSourceTest` 新增 7 个测试覆盖同 sentence 多 occurrence / 多章节来源 dedupe / fallback / carousel 不重复 / 只读保护。无业务逻辑变更。
- **子阶段 6 VocabularyQueryService 只读查询抽取（+150%）**：从 `VocabularyService` 抽取纯只读查询逻辑到新 `app/Services/VocabularyQueryService.php`。`VocabularyService` 改为代理调用，原有返回结构、public route 行为、Vue payload 完全不变。Controller 注入新 service。不碰 stage 更新、EncounteredWord 写入、ReviewLog、FSRS、导入逻辑。新增 characterization tests 防止回退。
- **子阶段 7 ChapterService 安全 DI 优化（+100%）**：`app/Services/ChapterService.php` 构造函数改为注入 `BookService` + `GoalService`，消除 `new BookService()` / `new GoalService()` 硬编码。`finishChapter()` 中 `(new GoalService())->updateGoalAchievement(...)` 改为 `$this->goalService->updateGoalAchievement(...)`。新增 `tests/Unit/ChapterServiceSafeDiTest.php` 6 个 characterization tests 锁定构造函数依赖 + 容器解析 + 源码扫描验证构造函数/finishChapter/processChapterText/deleteChapter 不再 `new BookService(`/`new GoalService(`。不改导入/tokenizer/processed_text/fallback/Book/Chapter 数据语义。
- **子阶段 8 文档同步与 GLM 1000% 结果治理（+150%）**：同步 `DOCUMENTATION_INDEX.md` / `current-working-handoff.md` / `vibe-coding-collaboration-rules.md` / `repo-architecture-hotspot-audit.md` / `spec-to-harness-candidates.md` / `mcp-chrome-local-smoke-playbook.md` / `morphology-test-sample-tracker.md`。
- **本轮明确未做（必须写清）**：第一轮未做阅读中刷卡评分；第一轮未做 per-occurrence lemma 落库；第一轮未改 FSRS；第一轮未改 ReviewLog；第一轮未真实调用 AI；第一轮未删除 legacy；第一轮未大规模重写 UI；第一轮未合并 VocabularySideBox / VocabularyBox / VocabularyBottomSheet；第一轮未删除 `conservativeFallbackLemma()`；第一轮未删除 Python-down fallback；第一轮未新增 migration；第一轮未清库。
- **后续高风险项规则**：后续如果要做高风险项（FSRS 调度 / ReviewLog 写入 / per-occurrence lemma 落库 / 阅读中刷卡评分 / 真实 AI 调用 / 删除 legacy / 大规模 UI 重写），必须 ADR / 需求冻结 / 单独任务，不得塞进 GLM 1000% 维护性轮次。
- **合规确认**：未读取/修改 `.env`；未修改 `AGENTS.md`；未处理 `.omo`；未修改 `.opencode`；未使用 `--force`；未 amend/rebase；未清库；未 migration；未改 FSRS；未改 ReviewLog；未实现阅读中评分；未 per-occurrence lemma 落库；未真实调用 AI；未删除 legacy；未大规模 UI 重写；未运行 notification script；未 DCP；未自动进入下一任务。
- **已知风险**：testing DB `linguacafe_fsrs_test` 在 45 个 `RefreshDatabase` 测试文件共享时存在并行竞争条件，已通过 `tests/bootstrap.php` + `flock` 进程锁治理。详见 `docs/plans/testing-db-health-playbook.md`。
- Did NOT enter the next task automatically.

## Recent Update: GM52-AIStudyCardDesktopWorkflowDeepModuleSplit-1000-4

- **Round type**: GM5.2 1000% closed-loop round 4 (deep-module split). **本轮同时完成检测 + 编码，不是只检测报告。不新增产品功能。不进入 V6。不实现手机端 / BottomSheet。不自动调用 AI。不改后端业务逻辑。**
- **起点 commit**：`03eec56 fix: restore vocabulary side box Chinese text`（基于 GitHub master 最新状态继续，未回退、未 force push）。
- **架构检测事实**（拆分前 `AiStudyCardDesktopWorkflow.vue`）：
  - 总行数：约 1020 行；
  - template 行数：约 480 行；
  - script 行数：约 540 行；
  - data 字段数量：25 个；
  - methods 数量：22 个；
  - 直接 axios / workflow service 调用：3 类（PendingWorkflowService / RecommendationParserService / GenerateCardsService）；
  - 直接 DOM / clipboard 逻辑：1 处 `document.createElement('textarea')` + `document.execCommand('copy')` fallback；
  - 同时承担的职责数量：10 类（待 AI 解释入口、pending/dismissed 列表、preview package、AI 推荐词 JSON 粘贴、推荐词解析去重默认不选、final candidates package、V5 生成学习卡确认对话框、生成结果展示、JSON 复制、`/reviews/senses` 跳转）。
- **拆分决策**：
  - 已拆：pending/dismissed 列表 → `AiStudyCardPendingListDialog.vue`（183 行）；AI 推荐词面板 → `AiStudyCardRecommendationPanel.vue`（132 行）；package JSON 展示与复制按钮 → `AiStudyCardPackagePanel.vue`（67 行）；V3-V5 预览弹窗（组合以上三者 + Result）→ `AiStudyCardPreviewDialog.vue`（253 行）；clipboard fallback → `AiStudyCardClipboardService.js`（70 行）。
  - 未拆及原因：V5 生成学习卡对话框 `AiStudyCardGenerateCardsDialog.vue` 保持原样（上一轮 feature island 已收敛，本轮不再拆）；顶层状态与跨子组件事件协调保留在 container（`AiStudyCardDesktopWorkflow.vue`）以保证单一入口、单一 Vuex 接入点；service 层（PendingWorkflowService / RecommendationParserService / GenerateCardsService）保持不变，避免本轮触碰后端契约。
- **拆分后 container 状态**：`AiStudyCardDesktopWorkflow.vue` 从 1020 行降至 448 行（满足 ≤450 限制，未触发 P2 例外说明）。template 仅保留入口按钮 + 3 个子组件 + 1 个 V5 对话框；script 保留 25 个 data 字段、22 个 methods（满足现有 `AiStudyCardDesktopWorkflowArchitectureGuardTest` 指纹）；`copyJsonToClipboard` 改为委托 `AiStudyCardClipboardService.copyTextToClipboard`。
- **新增文件**：
  - `resources/js/services/AiStudyCardClipboardService.js`（70 行，导出 `copyTextToClipboard(text)`，封装 navigator.clipboard + textarea fallback，不访问 Vue/Vuex/后端）；
  - `resources/js/components/Text/AiStudyCardPendingListDialog.vue`（183 行，props in / events out，不 import axios / Vuex）；
  - `resources/js/components/Text/AiStudyCardRecommendationPanel.vue`（132 行，props in / events out，不 import axios / Vuex）；
  - `resources/js/components/Text/AiStudyCardPackagePanel.vue`（67 行，props in / events out，不 import axios / Vuex / clipboard API）；
  - `resources/js/components/Text/AiStudyCardPreviewDialog.vue`（253 行，组合 RecommendationPanel + PackagePanel + GenerateCardsResult，不 import axios / Vuex）；
  - `tests/Feature/AiStudyCardDesktopWorkflowDeepModuleGuardTest.php`（27 项 / 221 assertions）。
- **修改文件**：
  - `resources/js/components/Text/AiStudyCardDesktopWorkflow.vue`（重构为薄 container，448 行）；
  - `tests/Feature/AiStudyCardV5DesktopArchitectureGuardTest.php`（适配：workflow 直接 import/render `AiStudyCardGenerateCardsDialog`，`AiStudyCardGenerateCardsResult` 由 `AiStudyCardPreviewDialog` 负责；workflow 渲染 `<AiStudyCardPreviewDialog>` 并传递 `:ai-generate-cards-result`）；
  - `tests/Feature/VocabularyBoxV5UiGuardTest.php`（同上适配）。
- **测试结果**（全绿，未把失败写成通过）：
  - `AiStudyCardDesktopWorkflowDeepModuleGuardTest`: 27 passed (221 assertions)；
  - `VocabularySideBoxChineseTextIntegrityTest`: 5 passed (42 assertions)；
  - `AiStudyCardDesktopWorkflowArchitectureGuardTest`: 22 passed (293 assertions)；
  - `AiStudyCardV5DesktopArchitectureGuardTest`: 17 passed (149 assertions)；
  - `VocabularyBoxV5UiGuardTest`: 16 passed (135 assertions)；
  - `AiStudyCardPendingItemTest`: 86 passed (484 assertions)；
  - `WordSenseTest`: 134 passed (555 assertions)；
  - `ReviewFsrsTest`: 63 passed (374 assertions)；
  - `SenseReviewDailyLimitsTest` + `SenseReviewSmokeDataCommandTest`: 19 passed (98 assertions)；
  - `SenseTokenPayloadTest`: 16 passed (45 assertions)；
  - `TestingDatabaseHealthConfigTest`: 6 passed (50 assertions)；
  - `TestingDatabaseHealthTest`: 6 passed (47 assertions)；
  - 合计 417 tests / 2159 assertions 全绿。
- **前端构建**：`npm run development` Compiled Successfully in 5209ms。
- **MCP Chrome 真实页面验收**（GLM 自己执行，未用 API 200 代替）：
  - 账号：本地管理员测试账号（不写入仓库 / 日志 / 报告）。
  - 宽屏 1920x900：`/chapters/read/6` → 点击 construed → VocabularySideBox 显示 → 中文文案正常无 mojibake → V1 mark pending → V3 list dialog → V3 preview dialog → V3 preview package → V4 粘贴/解析 AI 推荐词（默认不选）→ V4 勾选 agency → V4 final candidates package → V5 generate cards dialog → V5 填写中文释义（agency="代理机构"）→ V5 确认生成 → "已生成 1 张学习卡" → "进入 /reviews/senses 复习" 跳转成功。Network 全部本地，无外部 AI 请求；Console 无阻塞错误。
  - 半屏 900x900：`/chapters/read/6` → 点击 construed → VocabularyBox 显示 → 中文文案正常无 mojibake → 完整 V1-V5（construed="半屏解释" + agency="代理机构"）→ "已生成 2 张学习卡"。Network 全部本地，无外部 AI 请求；Console 无阻塞错误。
  - 未做 390x844 / 430x932 mobile viewport 主流程验收（当前产品无手机端）。
- **数据库事实**（MCP Chrome 过程中真实生成）：
  - WordSense 新增：宽屏 1 条（agency lemma）；半屏 2 条（construe / agency lemma）；
  - ReviewCard 新增：宽屏 #84 target_type=sense；半屏 #85/#86 均 target_type=sense；
  - ReviewLog：未新增；
  - legacy word ReviewCard：未新增；
  - 已有卡 FSRS：未变；
  - 无 duplicate 路径被误写成 created。
- **安全边界确认**：未读写 `.env`；未清库；未 `migrate:fresh` / `db:wipe`；未 DCP；未运行 notification script；未自动调用 AI；未接入 DeepSeek/OpenAI/任意 AI provider；未新增 API key；未写 ReviewLog；未改 FSRS；未重排已有 ReviewCard；未创建 legacy word ReviewCard；未删除 WordSense/ReviewCard/ReviewLog；未删除 legacy 兼容层；未修改 TextBlockGroup.vue / VocabularyBottomSheet.vue；未实现手机端 / BottomSheet V5；未进入 V6；未回滚 feature island；未重新制造 SideBox / Box 复制；未再次制造中文 mojibake。
- **commit / push**：`refactor: split AI study card desktop workflow modules`（无 `--force`，未提交 untracked 临时文件）。
- Did NOT enter the next task automatically. 等待网页端总流程设计师验收。

## Recent Update: GLM-ReadingInlinePreview-First-1

- **Round type**: GLM 800% round (reading inline sense preview / candidate confirmation first round). **不是阅读中评分。不写 ReviewLog。不改 FSRS。不做 per-occurrence lemma 落库。不真实调用 AI。**
- **第一硬原则**：代码安全性和稳定性优先于功能速度。本轮只允许做 read-only preview 和显式用户确认结构，禁止任何隐式学习数据写入。
- **本轮合计 800% 是 7 个子阶段进度合计，不是固定五条主线虚假上涨。**
- **子阶段 1 DB health + 现状锁定（+100%）**：确认 commit `cd97b5f`；DB health check 全绿（TestingDatabaseHealthConfigTest 6/6, TestingDatabaseHealthTest 6/6）；读取阅读页查词链路、WordSenseKnownSenseService（read-only）、ReviewLog/FSRS 写入链（`SenseReviewController::rate` → `ReviewCardService::recordReview` → `FsrsSchedulingService::schedule`，唯一 FSRS 入口；`ReviewCardService::resetCard` 第二写入入口）未被本轮触碰。
- **子阶段 2 read-only preview 后端契约（+150%）**：新增 `WordSenseKnownSenseService::previewInlineSenseCandidates(userId, language, lemma, surface, sentence)` 只读方法，返回 `{ lemma, surface, sentence, has_confirmed_senses, candidates, candidate_count, safety_flags{read_only, no_review_log_created, no_fsrs_changed, no_review_card_created, no_word_sense_created, no_ai_called}, ui_hint }`。新增 `GET /senses/inline-preview` endpoint on `SenseOccurrenceController::inlinePreview`，enforces `Auth::user()->selected_language` + 403 on language mismatch + 422 on empty lemma。无 POST/PUT/DELETE，无 migration，无 AI，无 FSRS，无 ReviewLog。
- **子阶段 3 前端 preview 区域（+150%）**：新增 `resources/js/components/Text/InlineSensePreviewPanel.vue` 只读子组件：props `{ lemma, surface, sentence, language, disabled }`；调用 `GET /senses/inline-preview`（唯一后端调用，只读）；显示候选预览 header + read-only chip + safety banner（"这是候选预览。本轮不会写入复习记录，不会改变 FSRS。"）+ surface/lemma/sentence + candidate list（POS/FSRS state/review count/sense_zh）+ 「是这个意思」/「不是这个意思」按钮；`setChoice()` 方法仅修改 `userChoice` 本地 data，注释明确 "Front-end only. Do NOT call any backend endpoint here."；无 `axios.post`/`axios.put`/rating route 引用。`WordSensesList.vue` 嵌入该组件（import + components 注册 + 模板插入），不改父组件 props/events 大结构。
- **子阶段 4 安全护栏测试（+150%）**：新增 `tests/Feature/InlineSensePreviewTest.php` 20 个测试 / 80 断言：endpoint 不写 ReviewLog/FSRS/WordSense/ReviewCard、不调用 AI、只返回当前用户+语言 confirmed senses、排除 pending/ignored/rejected、跨用户隔离、跨语言隔离、空 lemma/whitespace lemma 返回安全空态、大小写归一化、FSRS 字段正确、occurrence count 正确、safety_flags 6 条全 true、422 on empty lemma、403 on language mismatch。
- **子阶段 5 前端 guard 测试（+100%）**：新增 `tests/Feature/InlineSensePreviewUiGuardTest.php` 11 个测试 / 44 断言（PHP 源码扫描方式，因项目无 Vue 测试框架）：preview 文案「候选预览」存在、safety 文案「不会写入复习记录」「不会改变 FSRS」存在、无「立即评分」、无肯定性「写入复习记录」（避免误判 safety 文案中的否定句）、无「AI 已判断」、不触发 rating route（无 `axios.post`/`/review`/`/fsrs`/`/rate`）、choice 按钮仅设本地状态（`setChoice` 不含 axios）、按钮文案「是这个意思」「不是这个意思」存在、WordSensesList 嵌入 panel、legacy 文案「旧词条释义」等未重新出现。
- **子阶段 6 MCP Chrome 页面验收（+150%）**：账号 1816529781@qq.com，isolatedContext=GLM-ReadingInlinePreview-First-1，文章 `/chapters/read/14`。点击 5 个 token：men→man (0 candidates)、women→woman (0 candidates)、cities→city (手动创建 1 confirmed sense 后 1 candidate，测试「是这个意思」+「不是这个意思」按钮)、drank→drink (0 candidates)、eaten→eat (0 candidates)。所有 token 的 preview 面板正确显示「候选预览」+ read-only + safety banner + surface/lemma/sentence + 候选列表/空态文案。点击「是这个意思」后出现 alert "你的选择仅在本面板内记录，不会写入复习记录，不会改变 FSRS，不会创建词义或复习卡。"。Network 分析：点击按钮后无新 POST/PUT/DELETE 请求（所有 POST 都是现有功能如 dictionaries/search、vocabulary/word/update、senses/manual）。Console 仅有 Vue 开发模式 + WebSocket 降级（BROADCAST_DRIVER=log 预期）。无 API/axios/fetch 替代真实点击。
- **子阶段 7 文档同步（+100%）**：同步 `DOCUMENTATION_INDEX.md` / `current-working-handoff.md` / `reading-inline-review-and-example-pool-plan.md`。
- **本轮明确未做（必须写清）**：本轮不是阅读中评分；本轮没有 ReviewLog；本轮没有 FSRS；本轮没有真实 AI；本轮没有自动创建 WordSense/ReviewCard；本轮没有删除 legacy；本轮没有大规模 UI 重写；本轮没有合并 VocabularySideBox/VocabularyBox/VocabularyBottomSheet；本轮没有新增 migration；本轮没有 per-occurrence lemma 落库。
- **后续高风险项规则**：后续如果要做阅读中评分、写 ReviewLog、改 FSRS、per-occurrence lemma 落库、真实 AI 调用，必须单独 ADR / 需求冻结 / 单独任务，不得塞进 preview 前置结构轮次。
- **合规确认**：未读取/修改 `.env`；未修改 `.env.testing`；未修改 `AGENTS.md`；未处理 `.omo`；未修改 `.opencode`；未使用 `--force`；未 amend/rebase；未清库；未 migration；未改 FSRS；未改 ReviewLog；未新增 ReviewLog 写入入口；未实现评分；未 per-occurrence lemma 落库；未真实调用 AI；未自动创建 WordSense；未自动创建 ReviewCard；未删除 legacy；未删除 `ReviewCard::TARGET_WORD`；未删除 `conservativeFallbackLemma()`；未大规模重写 TextReader/TextBlockGroup；未合并查词组件；未运行 notification script；未 DCP；未自动进入下一任务。
- Did NOT enter the next task automatically.

## Recent Update: GM52-AIStudyCardV5-DesktopArchitectureConvergence-1000-1

- **Round type**: GM5.2 目标模式桌面端 AIStudyCard V5 架构收敛。**不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；保持 UI 文案和用户流程不变；保持 MCP Chrome 宽屏和半屏真实流程都通过。**
- **范围纠正**：上一轮网页端总流程设计师误把 `VocabularyBottomSheet.vue` 当作当前产品范围内的"手机端入口"，本轮纠正为：当前项目只支持电脑端，不做手机端，不补 BottomSheet 的 V5 流程，只处理宽屏 SideBox 与半屏 VocabularyBox 两个真实入口。
- **新增 3 个共享文件**：
  - `resources/js/services/AiStudyCardGenerateCardsService.js`：3 个纯函数 `buildGenerateCardItems(finalCandidatesPackage)` / `filterConfirmedGenerateCardItems(items)` / `generateAiStudyCards(axios, finalCandidatesPackage, confirmedItems)`。纯前端逻辑不写后端/DB/ReviewLog/FSRS。AI reason 只作参考说明不写入 sense_zh。user_selected 保留 chapter_id/text_block_index/sentence_index/sentence_text，ai_recommended 默认无 chapter_id 保持当前行为。sense_zh 必填初始化为空，sense_en 初始化为空允许为空。
  - `resources/js/components/Text/AiStudyCardGenerateCardsDialog.vue`：共享确认对话框组件。v-model + items/loading/error props + confirm event。展示候选词/lemma/source chip/AI reason 参考说明。中文释义必填，英文解释可选。"共 N 项，已填 M 项"计数。不直接请求后端，不感知父组件存在。
  - `resources/js/components/Text/AiStudyCardGenerateCardsResult.vue`：共享结果展示组件。result prop + go-to-sense-reviews/dismiss events。展示 created/skipped/duplicate/failed 四类结果 + sense_id/review_card_id + source_binding_status + "进入 /reviews/senses 复习"入口 + "这不是 AI 自动调用，是你粘贴 AI 返回内容后的人工确认生成"安全文案。不直接请求后端，不写 ReviewLog/FSRS/ReviewCard。
- **SideBox 与 VocabularyBox 接入方式**：保留原有入口位置与视觉文案，删除重复的 V5 确认弹窗模板/结果展示模板/buildGenerateCardItems 构造逻辑/confirmGenerateCards 请求逻辑，改为：父组件只持有 dialog open 状态 + result/loading/error + 调用共享 helper + 把数据传给共享组件 + 跳转 /reviews/senses。
- **新增/修改测试**：
  - 新增 `tests/Feature/AiStudyCardV5DesktopArchitectureGuardTest.php`（17 项测试 / 134 assertions）：覆盖共享文件存在性、两个父组件引用共享组件、不再包含大段重复 V5 模板（指纹检测：`推荐理由（参考说明，不是释义）：{{ item.reason }}` / `<v-dialog v-model="aiGenerateCardsDialog"` / `新建学习卡：` / `生成学习卡结果` / `source: 'user_selected',`）、不再重复 buildGenerateCardItems 逻辑、AI reason 不自动填 sense_zh、sense_zh 初始化为空、sense_en 允许为空、VocabularyBottomSheet.vue 不含 V5 流程、无外部 AI provider 字符串、无 ReviewLog/FSRS rating/legacy word card 创建调用、Service 暴露 3 个纯函数、Dialog/Result 只 emit 事件不直接调后端。
  - 重写 `tests/Feature/VocabularyBoxV5UiGuardTest.php`（16 项测试 / 104 assertions）：扫描目标从单一 VocabularyBox.vue 扩展到 4 个文件。
- **自动测试 9 个套件全绿（363 tests / 0 failures）**：AiStudyCardV5DesktopArchitectureGuardTest 17 passed (134 assertions) / VocabularyBoxV5UiGuardTest 16 passed (104 assertions) / AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)。
- **`npm run development` 编译成功**（5663ms，7.37 MiB app.js）。
- **MCP Chrome 真实双 viewport 回归验收全通过**：
  - **A. 宽屏 1920x900**：`/chapters/read/6` 点击 construed → VocabularySideBox → 完整 V5 流程（待 AI 解释 → 列表 → preview-package → 粘贴 AI 推荐词 JSON → final-candidates-package → 生成学习卡 → 共享 Dialog 渲染正确：安全文案 + sense_zh 必填 + sense_en 可选 + 计数显示 → 填写中文释义 → 确认生成 → POST /generate-cards 200 → 结果显示 0 created, 1 duplicate (释义 #73), source_binding_status=来源已绑定（合成 sentence_id）→ 进入 /reviews/senses → Network 全部 127.0.0.1:8000 无外部 AI 调用 → Console 仅 WebSocket 降级（pre-existing, non-blocking））。
  - **B. 电脑半屏 900x900**：`/chapters/read/6` 点击 construed → VocabularyBox → 同样完整 V5 流程，共享 Dialog/Result 均正常工作 → POST /generate-cards 200 → 结果 0 created, 1 duplicate → Network 全部本地 → Console 仅 WebSocket 降级。
- **数据库事实**：本轮验收触发的是 duplicate 路径（释义 #73 已存在），未新增 WordSense / ReviewCard / ReviewLog / WordSenseOccurrence；已有卡 FSRS 未重排。
- **明确未做**：不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；不改 TextBlockGroup.vue；不实现手机端/BottomSheet V5；不自动调 AI；不接 AI provider；不读写 .env；不新增 API key；不写 ReviewLog；不改 FSRS；不重排已有卡；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作逻辑；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收。
- **下一步仍由网页端总流程设计师决定**，不自动进入 V6。
- Did NOT enter the next task automatically.

## Recent Update: GM52-AIStudyCardV5-DesktopWorkflowFeatureIsland-1000-2

- **Round type**: GM5.2 目标模式 — 桌面端 AIStudyCard V1-V5 工作流收敛为独立 feature island。**不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；保持 UI 文案和用户流程不变；保持 MCP Chrome 宽屏和半屏真实 V1-V5 完整流程都通过。**
- **本轮解决的风险**：上一轮（DesktopArchitectureConvergence）只把 V5 生成学习卡的 Dialog/Result/Service 收敛为共享结构，但 V1（pending item 标记）→ V2（待解释列表/取消/恢复）→ V3（preview-package）→ V4（粘贴 AI 推荐词 JSON / parse / dedupe / final-candidates-package）的完整桌面端流程仍然散落在 `VocabularySideBox.vue` 和 `VocabularyBox.vue` 两个父组件里。改任一 V1-V4 规则都需要同时改两个组件，容易漏改一处造成宽屏和半屏行为漂移。
- **本轮落地**：
  1. 新增 `resources/js/components/Text/AiStudyCardDesktopWorkflow.vue`（约 1019 行 feature island 组件）：包含 V1-V5 全部模板/data/methods — `待 AI 解释` 按钮、`待 AI 解释列表` 按钮、待解释/已取消视图切换 + 列表/取消/恢复、preview-package 预览弹窗 + 全选/全不选 + 用户已选词列表 + AI 推荐词粘贴文本框 + 解析/清空 + AI 推荐词列表默认 unchecked + 全选/全不选 + 「生成最终候选包」+ 生成学习卡对话框 + 结果展示 + 「进入 /reviews/senses 复习」入口。通过 `computed: mapState('vocabularyBox', { _chapterId/_sentenceIndex/_sentenceText/_studyBase/_baseWord })` 从 store 获取上下文，不通过 props 传递上下文。
  2. 新增 `resources/js/services/AiStudyCardRecommendationParserService.js`（225 行纯函数模块）：导出 3 个纯函数 `buildUserSelectedKeys(finalCandidatesPackage)` / `parseAiRecommendations(rawJson, userSelectedKeys)` / `rededupeRecommendations(parsed)`。不 import Vue/Vuex/DOM。解析失败返回 `{ ok: false, error, recommendations: [] }`。
  3. 新增 `resources/js/services/AiStudyCardPendingWorkflowService.js`（195 行 API 封装模块）：导出 6 个函数 `createPendingItem(axios, payload)` / `listPendingItems(axios, params)` / `dismissPendingItem(axios, id)` / `restorePendingItem(axios, id)` / `buildPreviewPackage(axios, payload)` / `buildFinalCandidatesPackage(axios, payload)`；re-export 3 个 from `AiStudyCardGenerateCardsService.js`（`generateAiStudyCards` / `buildGenerateCardItems` / `filterConfirmedGenerateCardItems`），保持 V5 hardening 边界。
  4. 修改 `VocabularySideBox.vue`：删除 V1-V5 完整模板/data/methods，改为引入并挂载 `AiStudyCardDesktopWorkflow`。期间修复 mojibake 语法错误（上轮 PowerShell 脚本按行号删除代码后遗留的中文注释/字符串 `?/span>`、`?/v-btn>`、`?/v-chip>` 破损标签 + `setAiLookupError(...)` 缺失闭合引号导致 `npm run development` 失败）。
  5. 修改 `VocabularyBox.vue`：同 SideBox 删除并挂载 `AiStudyCardDesktopWorkflow`。
  6. 新增 `tests/Feature/AiStudyCardDesktopWorkflowArchitectureGuardTest.php`（22 项测试 / 293 assertions）：覆盖 feature island 文件存在性、两个父组件挂载该组件、不再包含重复 V1-V5 模板与 data 字段（指纹检测：`待 AI 解释`、`<v-dialog v-model="aiPendingListDialog"`、`<v-dialog v-model="aiStudyCardPreviewDialog"`、`aiPendingItems`、`aiPreviewPackage`、`aiFinalCandidatesPackage`、`aiSelectedRecommendationIndices`）、父组件只持有必要最小状态、`AiStudyCardRecommendationParserService` 暴露 3 个纯函数、`AiStudyCardPendingWorkflowService` 暴露 6 个函数 + re-export 3 个、解析失败安全失败、`AiStudyCardGenerateCardsDialog` 与 `AiStudyCardGenerateCardsResult` 仍由上一轮 V5 收敛保持不变、AI 推荐词默认 unchecked、AI reason 不自动填 sense_zh、sense_zh 初始化为空、sense_en 允许为空、`VocabularyBottomSheet.vue` 不含 V1-V5 流程、无外部 AI provider 字符串、无 ReviewLog/FSRS rating/legacy word card 创建调用。
- **自动测试 10 个套件全绿（385 tests / 0 failures）**：AiStudyCardDesktopWorkflowArchitectureGuardTest 22 passed (293 assertions) / AiStudyCardV5DesktopArchitectureGuardTest 17 passed (146 assertions) / VocabularyBoxV5UiGuardTest 16 passed (131 assertions) / AiStudyCardPendingItemTest 86 passed (484 assertions) / WordSenseTest 134 passed (555 assertions) / ReviewFsrsTest 63 passed (374 assertions) / SenseReview 19 passed (98 assertions) / SenseTokenPayloadTest 16 passed (45 assertions) / TestingDatabaseHealthConfigTest 6 passed (50 assertions) / TestingDatabaseHealthTest 6 passed (47 assertions)。
- **`npm run development` 编译成功**。
- **MCP Chrome 真实双 viewport 完整 V1-V5 回归验收全通过**：
  - **A. 宽屏 1920x900**：`/chapters/read/6` 点击 construed → VocabularySideBox → 完整 V1-V5 流程（POST /pending-items 200 item_id=5 → 列表显示「待解释 (1)」→ dismiss → 「已取消 (1)」→ restore → 「待解释 (1)」→ preview-package 200 safety_flags 全 true → 粘贴 AI 推荐词 JSON（agency/observation 2 条）→ 解析成功，2 条默认 unchecked，勾选 agency → final-candidates-package 200 → 生成学习卡对话框 → 填写 sense_zh → POST /generate-cards 200 → 「已生成 2 张学习卡」→ 数据库 WordSense #78/79 + ReviewCard #80/81 (target_type=sense) + 0 ReviewLog + 0 legacy word card → Network 30 个请求全部 127.0.0.1:8000 → Console 仅 WebSocket 降级）。
  - **B. 半屏 900x900**：`/chapters/read/6` 点击 construed → VocabularyBox → 导航栏折叠为「更多」菜单（半屏行为正确）→ 完整 V1-V5 流程，使用不同 AI 推荐词（progress）→ POST /generate-cards 200 → 「已生成 2 张学习卡」→ 数据库 WordSense #80/81 + ReviewCard #82/83 (target_type=sense) → Network 19 个请求全部 127.0.0.1:8000。
- **数据库事实**：本轮双 viewport 验收共新增 4 个 WordSense（#78/79/80/81）+ 4 个 ReviewCard（#80/81/82/83，全部 target_type=sense）+ 0 ReviewLog + 0 legacy word card；已有卡 FSRS 未重排。
- **关键安全契约两个 viewport 均确认**：AI 推荐词默认 unchecked ✓ / AI reason 不自动填 sense_zh ✓ / sense_zh 初始化为空 ✓ / sense_en 允许为空（实际为 null）✓ / 无外部 AI provider 调用 ✓ / 无 ReviewLog 写入 ✓ / 无 legacy word card 创建 ✓ / 所有 ReviewCard target_type=sense ✓。
- **明确未做**：不新增产品能力；不碰后端业务逻辑；不碰手机端/BottomSheet；不改 TextBlockGroup.vue；不实现手机端/BottomSheet V1-V5；不自动调 AI；不接 AI provider；不读写 .env；不新增 API key；不写 ReviewLog；不改 FSRS；不重排已有卡；不创建 legacy word card；不删除 WordSense/ReviewCard/ReviewLog；不删除 legacy 兼容层；不改 Source Context 主线；不改 ReviewCardManage 危险操作逻辑；不新增 migration；不清库；不 DCP；不 notification script；不处理 .omo/；不提交敏感文件；不把 API 200 当页面验收。
- **下一步仍由网页端总流程设计师决定**，不自动进入 V6。
- Did NOT enter the next task automatically.

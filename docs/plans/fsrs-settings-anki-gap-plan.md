# FSRS 设置页 Anki 对标与拆解计划

> **创建日期**：2026-06-25
> **关联里程碑**：[linguacafe-fsrs-roadmap.md](./linguacafe-fsrs-roadmap.md) → D 系列

---

## 1. 当前 LinguaCafe 已有能力

LinguaCafe 的 FSRS 设置页位于管理后台 (`/admin` → 间隔重复系统)，是一个全局设置页，以 `AdminReviewSettings.vue` 组件渲染。

**✅ 已有且真实可用的功能：**

- **Desired Retention 下拉选择**：提供 8 个选项（70%, 75%, 80%, 85%, 90%, 92%, 95%, 97%），默认 90%。通过 `v-select` 下拉调整，调用 `POST /settings/global/update` 保存 `fsrsDesiredRetention` 到数据库。保存成功后有绿色提示，说明新评分会使用该保持率但已排程卡片不会自动重排。

- **保存按钮**：页面底部有蓝色"保存 FSRS 设置"按钮，带 loading 状态和成功/失败提示。

- **FSRS 说明卡片**：页面顶部有蓝色信息卡片，用通俗中文解释 FSRS 是什么：系统根据评分、稳定度、难度和复习历史自动计算下次复习时间。明确说明"不建议也不允许手动编辑参数"，"如需重新开始，应使用重置卡片为新学状态"。

- **FSRS 统计总览**：另一张卡片展示当前语言的词义复习卡统计，按语言区分：

  - 概况（总词义卡 / 启用中 / 已归档 / 当前到期）
  - 状态分布（新卡 / 学习中 / 复习中 / 重新学习）
  - FSRS 熟练度（平均稳定度 / 平均难度 / 总遗忘次数 / 今日已复习 / 今日重置）

  统计数据来自 `GET /review-cards/stats` 端点（`ReviewStatsService` 实时计算）。

- **旧 SRS 设置折叠区**：可展开/折叠的旧版间隔复习间隔设置，仅影响 legacy word card 和 phrase，另有警告说明词义复习卡不受此影响。

- **卡片重置**（⚠️ 不在本页）：单卡/批量重置功能在 `/review-cards/manage` 管理页实现（操作列"更多"菜单 → 重置），不在 FSRS 设置页本身。

**⚠️ 当前是占位文字的功能：**

- **参数来源**：轻量显示"当前使用默认参数 / 还没有优化过"，仍未接入真实参数来源、版本号或优化历史。
- **参数编辑**：显示"暂未开放"，完全不可交互。
- **参数优化**：已有按钮入口和后端资格检查 preflight，但不会真实计算或保存参数。

---

## 2. 与 Anki FSRS 设置的差异

| 能力 | Anki | LinguaCafe 当前 | 建议 |
|------|------|----------------|------|
| **Desired Retention** | 滑块 (0.70-0.99)，默认 0.90，实时显示复习负担估算 | 下拉 (70%-97%), 默认 90%，有"越高复习负担越重"说明，无负担估算 | 增加每个 retention 值的负担预估，帮用户理解后果 |
| **Help Me Decide** | 用模拟结果帮用户选 retention，回答"我每天大概复习多少" | ❌ 无此功能 | D.2-c 可做简易版：按当前卡片数 × retention 对应复习频率做粗略预估 |
| **参数优化** | 根据复习历史自动生成更适合用户的 FSRS 参数，需积累一定复习数据 | ⚠️ 已有按钮和 preflight，但不真实计算或保存参数 | D.3-a 可实现：调用 fsrs-rs-php 的 optimize 方法，提供结果预览与确认保存 |
| **参数健康检查** | 检查复习历史是否足够优化，数据不足时不建议优化 | ✅ 已有第一版：`GET /settings/fsrs/optimization-status`，阈值 300 条真实 sense review log | 后续 D.3-a 复用检查，并在接入真实优化前评估 fsrs-rs-php 输入格式 |
| **重排已有卡片** | 改 retention 或参数后可选择重排，默认不自动重排，强提醒会产生大量到期卡 | ❌ 无此功能，retention 保存后明确说"不自动重排" | D.4-a 可做手动触发：显示影响卡片数量，用户强确认后才执行 |
| **模拟未来复习负担** | 展示"每天大概要复习多少张 / 花多少时间" | ❌ 无此功能 | 优先级较低，D.2-c 做简易版后可在后续版本升级为图表 |
| **参数来源 / 版本** | 显示当前参数来源（默认/优化）、优化时间 | 固定文字"fsrs-rs-php 默认参数"，不反映真实状态 | D.2-d 可从数据库读取参数来源标记和优化时间 |
| **普通解释文案** | 英文，偏技术但易懂 | 中文，当前解释已经比较好 | 保持现有中文风格，补充 retention 影响说明即可 |
| **重置卡片入口** | 不在此页面（在浏览器/卡片管理） | 显示"后续开放"但实际上管理页已有，说明过时 | 更新文案，提供跳转链接到 /review-cards/manage |

---

## 3. 产品原则

这些原则决定了 D 系列"做什么"和"怎么做"：

1. **用户不需要懂稳定度 / 难度公式。** 设置页的目标是让用户做决策，不是让用户学算法。所有说明和提示用"后果"导向的语言（"这样做会多复习 / 少复习"），不用公式参数导向的语言（"改变 w9 影响难度插值"）。

2. **不允许手动编辑卡片熟练度参数。** stability、difficulty 等数值由算法自动维护，设置页永远不会提供"手动调 stability"的功能。这是 FSRS 设计的核心前提。

3. **高级功能分阶段开放。** 第一版做信息展示和简单预估，后续版本逐步加入参数优化、模拟器、重排。避免一下子给用户太多复杂选项。

4. **所有会大幅改变到期时间的操作必须强提醒。** 参数优化、卡片重排这类操作可能产生几十张突然到期的卡，必须向用户说明影响量级，并要求确认。

5. **第一目标是"让用户知道自己改 retention 会带来什么后果"。** 当前用户改完 retention 后看不到任何影响，直到下次复习才体会出来。"帮我决定"不只是帮你决定，更是帮你理解决定的影响。

---

## 4. 小步骤拆解

以下任务按优先级排序，编号延续 [linguacafe-fsrs-roadmap.md](./linguacafe-fsrs-roadmap.md) 的 D 系列。

### D.1-scout（✅ 本任务已完成）

FSRS 设置页 Anki 对标侦察 + 独立拆解文档建立。完成本文件。

---

### D.2-a：FSRS 设置页信息架构重排（✅ 已完成，2026-06-25）

**目标**：把设置页从"功能说明 + 占位文字"变成真正的"控制面板"布局。

**已完成**：
- 重新组织卡片布局为三个清晰区域：
  1. **复习目标** — Desired Retention 下拉 + 保存按钮 + 说明
  2. **当前 FSRS 状态** — 保留所有统计总览（13 种指标）
  3. **高级工具** — 参数来源/参数编辑/卡片重置/参数优化，含说明和跳转入口
- 更新过时文案：
  - "卡片重置 — 后续开放" → "已在复习卡管理页开放" + "前往复习卡管理页"按钮
  - "参数优化 — 后续开放" → "暂未开放" + 说明后续条件
  - "参数编辑 — 暂未开放" → "不开放手动编辑" + 说明原因
  - 删除页面底部"FSRS 参数优化和卡片重置功能会在后续版本加入"过时文案
  - 删除原 FSRS 说明 alert（内容分散到各区域）
- 不改算法，不新增后端接口，不新增路由。
- 保留旧 SRS 设置折叠区。

**D.2-a-fix 补充**（2026-06-25）：
- 高级工具改为默认折叠，标题旁提示"参数优化、手动参数、卡片重置等低频操作，需要时再打开。"
- 高级工具内部顺序调整为：自动优化参数（第一）→ 参数来源（第二）→ 手动编辑参数（第三）→ 卡片重置（第四）
- "自动优化参数"显示"暂未开放"，说明后续根据复习记录自动优化
- "手动编辑参数"改为"暂未开放，后续可以作为高级功能单独评估"，不再写死永不开放
- "前往复习卡管理页"按钮从表格行中移到高级工具内容区左下角
- 不实现参数优化，不实现手动参数编辑，不改算法

---

### D.2-b：Desired Retention 帮助说明增强（✅ 已完成，2026-06-25）

**目标**：帮用户理解不同 retention 值的实际影响。

**已完成**：
- 在 Desired Retention 下拉框下方增加动态说明区域。
- 根据当前选择显示对应的中文影响描述（纯前端 computed 计算）：
  - 70% → "复习压力最低，但会更容易忘，适合临时减负。"
  - 75% → "复习量较少，适合想保持轻量学习的人。"
  - 80% → "偏轻松，能减少复习次数，但记忆保持会下降。"
  - 85% → "负担适中偏轻，适合不想被复习压住的人。"
  - 90% → "记忆效果和复习负担比较平衡。"（+ "推荐默认值" chip）
  - 92% → "记得更稳一些，但每天复习会变多。"
  - 95% → "追求更牢固记忆，复习负担会明显增加。"
  - 97% → "非常高的保持率，复习压力可能很大，请谨慎选择。"
- 90% 使用 v-chip 标注为"推荐默认值"。
- 低保持率（70%、75%）保留可选，文案说明"更容易忘"。
- 保留保存功能不变。
- 不做模拟器，不做精确计算。纯文案引导。
- 不改后端。

---

### D.2-c：复习负担预估第一版（✅ 已完成，2026-06-25）

**目标**：给用户一个粗略的"改了 retention 后每天大概要多复习多少"的概念。

**已完成**：
- 在"复习目标"区增加小型复习负担预估框，放在 Desired Retention 说明下方。
- 使用当前已有 stats（enabled / due / reviewed_today）做粗略估算。
- 使用倍率映射（70%→0.55 ~ 97%→1.90）简单估算每日复习量，显示范围（±25%）。
- 无启用词义卡时显示"先不用担心复习负担"。
- 90% 时提示"比较平衡的默认选择"。
- 低于 90% 提示"会轻松一些，但也更容易忘"。
- 高于 90% 提示"记得更稳，但复习会更密"。
- 底部固定说明："粗略预估，仅帮助你感受负担，不会重排已有卡片。"
- 纯前端（computed 计算），不改后端，不改排程，不画图表，不做完整 Help Me Decide。

---

### CODEX-FSRS-1：自动优化入口与资格检查（✅ 已完成，2026-06-25）

**目标**：参考 Anki 的 FSRS 设置结构，在 LinguaCafe 先落地安全的"自动优化参数"入口，但不在缺少可靠优化实现时伪装成功。

**已完成**：
- 修正 roadmap latest commit：D.2-c 后基线为 `9dad39d`。
- FSRS 设置页形成"简单模式 / 高级工具"雏形：
  - "复习目标"和"当前 FSRS 状态"默认显示，属于常用设置。
  - "高级工具"默认折叠，需要时展开。
  - 高级工具第一项为"自动优化参数"。
- 新增按钮文案：`根据我的复习记录优化`。
- 新增 `GET /settings/fsrs/optimization-status`：
  - 返回 `review_count` / `min_required` / `can_optimize` / `message`。
  - `min_required = 300`。
  - 记录不足时提示：`复习记录还不够，先继续复习一段时间再来优化。`
  - 记录足够但尚未接入真实参数计算时提示：`已经有足够记录，但自动优化还需要下一步接入参数计算。`
- 新增 `POST /settings/fsrs/optimize`：
  - 第一版只做 preflight。
  - 始终返回 `optimized=false`。
  - 不写入 FSRS 参数、不修改 Desired Retention、不重排已有卡片。
- `review_count` 统计范围：
  - 只统计当前用户。
  - 只统计当前语言。
  - 只统计 `ReviewCard.target_type=sense`。
  - 通过 join `word_senses` 限定 confirmed WordSense。
  - 排除 `source=reset`。
  - 排除 legacy word card、其他用户、其他语言、已拒绝或无法关联的记录。
- 参数来源轻量显示为：
  - `当前使用默认参数`
  - `还没有优化过`

**未完成 / 后续边界**：
- 没有真实优化参数，因为当前任务没有完成 fsrs-rs-php optimize 输入映射、性能评估和结果验证。
- D.3-a 才是真实参数优化主线：需要接入参数计算、结果预览、确认保存和测试覆盖。
- D.2-d 仍负责真实参数来源与版本说明，不因本次轻量文案而标记完成。
- D.4 仍负责重排已有卡片，本次没有实现重排。
- 手动编辑参数仍是未来高风险功能，本次没有实现，也不标记完成。

---

### D.2-d：参数来源与版本说明（✅ 已完成，2026-06-26）

**已完成**：
- 后端 `resolveFsrsParameterSource()` 读取真实 settings（user_id=-1），返回 6 字段：parameters_source / source_label / last_optimized_at / parameters_count / has_optimized_parameters / warning
- 前端根据状态展示对应文案：默认 → "当前使用默认参数 / 还没有保存过优化参数 / 参数数量：19"；已优化 → "当前使用已优化参数 / 最近优化时间 / 参数数量：21 / 只影响新评分，不重排"
- 4 个新测试（28 total，162 assertions）：默认、已优化、JSON 异常、自定义来源
- 不新增 migration，不保存参数，不重排卡片
- **D.2-d-fix (2026-06-26)**：`resolveFsrsParameterSource()` 增加 `decodeSettingValue()` helper 解析 json_encoded 来源/时间字段；optimized 状态文案改为 v-chip "正在优化参数"；29 个测试全部通过（+1 confirm→status 检查）

---

### D.3-scout：参数优化可行性侦察（✅ 已完成，2026-06-25）

**目标**：研究 fsrs-rs-php 是否支持 optimize，需要多少 ReviewLog。

**侦察结论**：
- `compute_parameters()` API **已确认可用** — 接收 FSRSItem 训练集，返回 19 个优化权重
- ReviewLog 数据完全满足输入要求（rating→1-4 映射，delta_t 可计算）
- 无需新增 Composer 依赖或 migration
- 数据阈值维持 300
- 输出独立计划文档 `fsrs-parameter-optimization-plan.md`
- 未实现真实优化

---

### D.3-a：参数优化第一版（✅ 已完成，2026-06-25）

**前置条件**：D.2-d（参数来源字段）+ D.3-scout（可行性确认）— 均已完成。

**已完成**：
- `SettingsService::computeFsrsOptimizationPreview()` — 构建 train set（FSRSItem[]），调用 `compute_parameters()`，返回 current vs optimized 参数对比 JSON。
- `SettingsController::optimizeFsrsParameters()` — 返回优化预览（200），不保存参数。
- 17 个测试：预飞流程、隔离过滤（word/reset/user/language/unconfirmed sense）、无副作用验证（不保存参数、不重排卡片）、rating 映射（again/hard/good/easy）。
- 不保存参数，不重排卡片（留给 D.3-b / D.4）。

---

### D.3-b：参数确认保存（✅ 已完成，2026-06-25）

**完成内容**：
- `SettingsService::applyFsrsOptimizedParameters()` — 服务端重新计算预览，验证参数，DB::transaction 保存 4 个 settings（fsrs_parameters, fsrs_parameters_source, fsrs_parameters_optimized_at, fsrs_parameters_previous）。
- `SettingsController::optimizeFsrsParameters()` 接受 `confirm=true`，调用 apply 方法。
- Vue `confirmApplyFsrsParameters()` — 按钮 + loading + 成功提示，成功后重新运行 preflight。
- 24 个测试（+7 新增）：不充分记录拒绝、4 settings 保存验证、metadata 正确、不重排、无需 preflight 重新计算、preflight 失败不保存、global user_id=-1 隔离。
- 不重排卡片（留给 D.4）。

---

### D.3-c：前端优化预览展示（✅ 已完成，2026-06-25；FSRS-D3-c-recovery 恢复提交 2026-06-25）

**完成内容**：
- AdminReviewSettings.vue 新增优化预览卡片。
- 展示 review_count / card_count / parameter_count 统计。
- 参数摘要（变化数量、最大幅度）+ 可展开参数明细表格。
- 处理 19/21 参数差异，缺失值显示 `—`。
- 安全提示：不保存参数、不重排卡片、不改变复习安排。
- 无"确认应用"按钮，展示"暂不应用，等待下一步"。
- 记录不足时保持原有提示。

---

### D.4-scout：重排已有卡片可行性侦察

**目标**：评估改 retention 或参数后重排卡片的影响和风险。

**要做什么**：
- 只读研究：fsrs-rs-php 的 `reschedule` 方法。
- 评估影响量级：以当前用户数据模拟，看重排会导致多少卡片突然到期。
- 评估后端实现复杂度：需要逐卡调用 `fsrs.rs.repeat()` 还是可以批量？
- 不改代码。

---

### D.4-a：重排已有卡片手动工具

**前置条件**：D.4-scout 可行性确认。

**目标**：让用户可以在改 retention 或优化参数后手动选择重排卡片。

**要做什么**：
- 触发按钮放在高级工具区，明确标注"⚠️ 高级操作"。
- 强确认弹窗：显示"重排将影响 N 张启用中卡片，可能产生 M 张今日到期卡。此操作不可撤销。确认重排？"
- 后端逐卡调用 reschedule，更新 fsrs_due_at / fsrs_stability / fsrs_difficulty。
- 保留 ReviewLog（重排不产生日志，但也不删除现有日志）。
- 必须保留现有 `user_id` / `language_id` / `target_type=sense` / `confirmed` 安全约束。

---

## 5. 不做范围

以下功能明确不在 D 系列范围，除非后续单独侦察：

- ❌ 手动编辑单卡 stability / difficulty。
- ❌ 删除 ReviewLog。
- ❌ 绕过 sense-only（不做 word card 相关任何操作）。
- ❌ 修改 ReviewCard.target_type 语义。
- ❌ 修改 CSV / Anki TSV / JSON 导出。
- ❌ 修改复习评分按钮语义（again/hard/good/easy）。
- ❌ AnkiConnect 同步。
- ❌ 多 deck / preset 系统（除非后续单独侦察）。
- ❌ 移动端原生设置页。
- ❌ 图表化复习负担趋势图。
- ❌ 用户间参数共享 / 参数模板。

---

## 6. 推荐下一步

**目标完成**：CODEX-FSRS-1 — 自动优化入口与资格检查（✅ 已在本轮完成）。

**下一步推荐：D.3-d — FSRS 调度集成（使用优化后的参数实际调度复习卡片）。**

**理由**：
- D.2-d 参数来源已完成：`resolveFsrsParameterSource()` + 28 个测试全部通过。
- D.3 系列（scout→a→c→b）全部完成：参数可计算、可预览、可确认保存、来源可真实展示。
- 现在优化参数已保存在 settings 中，下一步是在实际的 FSRS 调度流程中读取并替代默认参数。
- 详见 `fsrs-parameter-optimization-plan.md`。
